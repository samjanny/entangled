# Release engineering

This document describes the release engineering conventions for the Entangled specification repository. It is not part of the protocol specification.

## Tag naming

Tags follow these patterns:

| Type | Format | Example |
|---|---|---|
| Release candidate | v\<MAJOR\>.\<MINOR\>-rc.\<N\> | v1.0-rc.1, v1.0-rc.2 |
| Final release | v\<MAJOR\>.\<MINOR\> | v1.0, v1.1 |
| Patch release (spec text only) | v\<MAJOR\>.\<MINOR\>.\<PATCH\> | v1.0.1, v1.0.2 |

All tags use a leading "v". The release-candidate suffix uses a dot before the number ("rc.1", not "rc1") for consistency with the semver-style pre-release format.

Tag uniqueness is normative: a tag once published is immutable. To correct a mistaken tag, create a new tag with a new name; do not rewrite or move existing tags.

## Release types

### Release candidate

A release candidate marks a state in which the specification is considered substantially complete and ready for community review, but is not yet final. Multiple rc tags are expected during stabilization.

Each rc must have a corresponding entry in the repository describing what changed since the previous tag.

### Final release

A final release marks a stable specification version. After a final release, the specification text for that version is frozen except for patch releases that correct errors without changing protocol behavior (see §11 for the spec release rules).

### Patch release

A patch release corrects errors in the specification text without changing wire-format behavior, validation rules, error codes, or cryptographic primitives. Patch releases follow the rules in §11 "Spec release".

## Release process

1. Apply substantive changes via pull request review or direct commits to the main branch.
2. Verify cross-references and run any available automated checks.
3. Update this file's release notes section if applicable.
4. Tag the commit with the appropriate name from the table above.
5. Push the tag to the origin remote.
6. Verify the tag appears in the repository tag list.

Tag deletion is reserved for accidental or malformed tags only and requires explicit communication if the tag has been published or referenced externally.

## Release notes

Release notes for each tag are added below as releases are made. Newest entries first.

### v1.0-rc.19 (planned - after rc.18 soak)

This section describes the changes that will land in v1.0-rc.19. The changes are on `main` ahead of the rc.18 tag; rc.19 will be tagged once the rc.18 soak window concludes without blockers.

Date: TBD

Changes since v1.0-rc.18:

This rc lands three audit-closing workstreams: a content-tree mechanism (Lotto 11, N45-N49) that lets the publisher commit to per-path content hashes in the manifest, closing audit findings C-1 (K_runtime forgery) and H-1 (content rollback); a SHOULD-to-MUST elevation on cross-session migration history (Lotto 12, N50), closing H-4; and a canary-block default flip (Lotto 13, N51), closing M-1. Wire format gains two optional fields (`content_root` on manifests, `seq` on content documents), one new non-signed resource (`/content_index.json`), and one new reserved path. The diagnostic catalog gains seven new error codes. One SHOULD is elevated to MUST with cross-session persistence required. One behavioral default is inverted (expired-canary block-by-default with per-session override).

**Lotto 11 - Content tree (N45-N49)**

**§06 Manifest - `content_root` field (N45)** - New optional top-level manifest field `content_root` (string, `sha-256:<base64url>`, 51 characters). When present, commits the manifest (and by extension `K_publisher`) to the SHA-256 digest of the exact bytes of `/content_index.json`. Absent when the publisher does not use the content index mechanism. Per the rc-cycle additive-field rule (§11 N38), this optional field is added without bumping `spec_version`. Ceremony interaction: `content_root` is updated at each canary rotation; `K_publisher` remains offline between ceremonies.

**§02 Content document - `seq` field and content index format (N46)** - New optional top-level content document field `seq` (integer >= 1, monotonic per path). New closed-structure content index format at `/content_index.json` defined in §02: a non-signed JSON document with `entries` mapping paths to `{seq, hash}` pairs. Size cap 1 MiB. Parsing restrictions inherited from Entangled document rules (strict UTF-8, no BOM, no duplicate keys, integer grammar per §04, strict base64url). Path `/content_index.json` reserved in content `path`, transaction `in_response_to`, and image `src` across §02 and §03.

**§09 Transport - content index fetch (N47)** - New fetch section defining GET `/content_index.json` with `Content-Type: application/json`, `Content-Length` required, no `Content-Encoding` or `Transfer-Encoding` (hash binding requires exact bytes).

**§10 Client behavior - content index verification (N48)** - Stage 9 gains a content index sub-step for manifests with `content_root`: fetch, hash-verify, validate structure, cache. Per-content-document verification at Stage 9 enforces exact match for indexed paths: `doc_seq == idx_seq AND doc_hash == idx_hash` is the only accept; `seq < idx_seq` is `E_CONTENT_SEQ_ROLLBACK`, `seq > idx_seq` is `E_CONTENT_SEQ_UNCOMMITTED`, hash mismatch at equal seq is `E_CONTENT_HASH_MISMATCH`, missing `seq` on indexed path is `E_CONTENT_SEQ_MISSING`. Paths not in the index are accepted without content-index verification (unprotected, documented). Indexed paths are frozen between ceremonies.

**§11 Diagnostic catalog - content index diagnostics (N49)** - Seven new error codes: `E_CONTENT_INDEX_FETCH_FAILED`, `E_CONTENT_INDEX_HASH_MISMATCH`, `E_CONTENT_INDEX_INVALID`, `E_CONTENT_SEQ_MISSING`, `E_CONTENT_SEQ_ROLLBACK`, `E_CONTENT_SEQ_UNCOMMITTED`, `E_CONTENT_HASH_MISMATCH`. All at stage 9, severity error. §00 limitation N39 updated to "Partial detection" (indexed paths now protected). §01 three new glossary entries (Content index, Content root, Content sequence number). §01 Manifest entry updated. docs/design-decisions.md revision/content_hash note updated.

**Lotto 12 - Migration history SHOULD to MUST (N50)**

**§10 Cross-session migration history - SHOULD to MUST (N50)** - All SHOULD-level migration-history requirements in §10 elevated to MUST. Implementations MUST record Adoption and Replacement events, MUST persist them across sessions (volatile-only storage is non-conformant), and MUST consult the recall window (30-day recommended, 7-day MUST floor, 365-day MUST ceiling, zero not permitted) when processing new migration announcements. The storage backend (serialization format, database technology) remains implementation-defined; cross-session persistence is required. §00 limitation revised. §01 glossary updated.

**Lotto 13 - Canary-block default (N51)**

**§08 Canary expired behavior - block default with per-session override (N51)** - The §08 Expired state default flips from warning-only rendering to rendering block. Clients MUST refuse to render current content when the canary is Expired; the content area is blank or shows a client-generated placeholder; the chrome displays the block notice and per-session override control. The override requires affirmative user action, applies for the current session only, and does not suppress the chrome warning. §10 canary state table updated (Expired: "No, blocked; per-session override available"). "Expired-canary-block mode" (previously opt-in) replaced by "Permissive-canary mode" (opt-in to the old warning-only behavior). §00 limitation updated.

**Lotto 14 - Security hardening (N52-N55)**

**§10 Historical content - publication-existence proof (N52)** - Historical content verification gains a fifth mandatory condition: the document's `(path, seq, hash)` tuple must be present in a content index previously verified under the authorizing manifest, or the client must have a rendering record for the document under that manifest. Without this condition, an attacker who exfiltrates a former `K_runtime_priv` can fabricate historically-authentic-looking documents that were never published. When neither proof source is available, the client MUST reject with new diagnostic `E_HISTORICAL_NO_PUBLICATION_PROOF` (§11).

**§05, §07 K_runtime compromise and state persistence (N53)** - The §05 compromise-impact paragraph for `K_runtime` now explicitly acknowledges that request-state entries installed by a compromised runtime key survive rotation (TTLs up to 90 days). New §07 section "State and runtime-key rotation" requires clients to mark request-state entries as `runtime_superseded` when the authorizing `K_runtime` is rotated, suspending their inclusion in submit requests while retaining them for user inspection. Fresh values require fresh consent under the new key. Client-only state is unaffected.

**§07 Request-state publisher-wide security note (N54)** - The existing advisory on publisher-wide request state is strengthened: the SHOULD NOT on endpoint-confined credentials is elevated to MUST NOT; a new "Security implication" block explains the cross-endpoint credential exposure; the examples list is trimmed to cases where publisher-wide visibility is acceptable.

**§08, §10, §11 Canary runtime-key rotation proof (N55)** - §08 now requires that a new manifest's `runtime_pubkey` be distinct from the immediately preceding manifest's. Reuse triggers `E_CANARY_RUNTIME_REUSE` (§11), a new error-severity diagnostic. §10 Stage 8 pipeline summary updated to include the rotation check. Without this rule, a publisher (or attacker holding `K_runtime`) can maintain the same key indefinitely behind fresh-looking canaries.

**Lotto 15 - errata (N56-N58)**

A seventh-pass audit (multi-agent verification of audit findings against current spec text) surfaced three textual issues with no impact on wire format, schema, or the diagnostic catalog: a typo in a `details.reason` identifier, a contradiction between §10 and §11 on the chain-depth handling, and an ordering defect in the §08 canary ceremony step list. They are aggregated into Lotto 15 on top of Lotto 14.

**§11 `E_ORIGIN_INVALID.details.reason` typo - `not_after_not_after_issued_at` -> `not_after_not_later_than_issued_at` (N56)** - The §11 structured diagnostic format for `E_ORIGIN_INVALID` listed two `reason` identifiers, one of which contained an obvious stutter (`not_after_not_after_issued_at`). The intended identifier is `not_after_not_later_than_issued_at`, mirroring the prose constraint "the declared `not_after` is not strictly later than `canary.issued_at`". The identifier is renamed in §11. Textual; the diagnostic code itself and the underlying check are unchanged.

**§11 `E_MIGRATION_INVALID` triggers - chain-depth removed, `chain_cycle` added, structured `details` documented (N57)** - The §11 row for `E_MIGRATION_INVALID` previously listed "chain depth exceeded" as one of the triggering conditions (qualified as "for clients enforcing the chain-depth rule"). This contradicted §10:442, which states that reaching the automatic chain-depth limit produces a *pending user action* state (recoverable by user confirmation), not a hard rejection with a diagnostic. The contradiction was also a category error: §11 defines `E_MIGRATION_INVALID` as a property of the document ("structurally valid JSON but fails semantic checks"), whereas chain-depth-exceeded is a property of the client's local traversal budget, configurable per §10:435,440 and therefore non-deterministic for the same input across conforming clients. The §11 row is updated by swap: the "chain depth exceeded" trigger is removed, the `chain_cycle` trigger (defined in §10:436 but previously absent from the §11 trigger list) is added, and a new clarifying sentence states that reaching the automatic chain-depth limit is not a semantic failure and does not produce this diagnostic. A new structured-diagnostic-format block for `E_MIGRATION_INVALID` is added immediately after the `E_MIGRATION_MISMATCH` block, documenting `details.reason` (drawn from `self_pointer`, `announced_at_after_updated`, `carrier_mismatch`, `chain_cycle`), `announcing_origin_address`, and `successor_origin_address`. §10 is not changed in behavior; the §10:435 prose is separately disambiguated under N57 to state explicitly that "automatic adoption" in the chain-depth subsection means following a hop without re-prompting on the basis of a confirmation already obtained earlier in the flow, never a first-time adoption without confirmation (the initial `A -> B` adoption always requires user confirmation per §10:404, §10:410, §10:411). The disambiguation does not change client behavior; it removes a literal-reading inconsistency between §10:435 ("automatically adopt one hop without re-prompting") and §10:404/410/411 (user-confirmation MUST for all trust states). Resolves the cross-section contradiction without changing wire format, validation behavior, or the diagnostic catalog.

**§08 Canary ceremony step ordering - reversible signing cutover precedes irreversible manifest deploy (N58)** - The §08 canary ceremony step list had two defects. First, steps 5 and 6 both stated "Deploy the new manifest", with step 5 also instructing the operator to "make the new `K_runtime_priv` available to the publishing infrastructure" - a duplicated deploy instruction. Second, the configuration of the publishing infrastructure to sign with the new `K_runtime_priv` was placed in step 7, *after* the manifest deploy in step 6. Steps 5, 6, 7 are restructured: step 5 transfers `K_runtime_priv` to the publishing infrastructure and configures the infrastructure to sign new content and transaction documents with it, without deploying the new manifest yet; step 6 deploys the new manifest at `/manifest.json`; step 7 destroys the previous `K_runtime_priv`. The structural reason: a signature-verification window is inherent to single-runtime-key rotation (clients holding a cached prior manifest verify against the previous `K_runtime.pub`; clients fetching the new manifest verify against the new `K_runtime.pub`; the publisher's signing configuration can only point at one private key at a time, and no protocol mechanism in v1 supports dual-key overlap on the published side). The reordering does not eliminate the window; it minimizes its duration when steps 5 and 6 are performed in immediate succession (ideally atomically), and - more importantly - it places the *reversible* step (the signing-configuration switch in step 5, which can be reverted at the publisher's discretion as long as no client has observed the new manifest) before the *irreversible* step (the manifest deploy in step 6, which the moment any client observes it commits the new `canary.issued_at` under §08:65 anti-downgrade enforcement and prevents the publisher from rolling back to the previous manifest under the same `K_publisher`). The pre-N58 order had the irreversible step first: if the signing cutover then failed, the publisher was trapped between a deployed manifest whose `K_runtime.pub` no infrastructure could match and an anti-downgrade rule that prevented serving the previous manifest. The new order eliminates that trap. No wire-format or schema change.

**Lotto 16 - corpus coverage expansion (no spec changes)**

A seventh-pass audit (multi-agent verification) also identified eight previously-uncovered diagnostic codes that are reachable within the existing vector schema. Seven new vectors are added to `corpus/corpus.json` and `corpus/vectors/`. The remaining uncovered code in the actionable set, `E_SIG_MALFORMED`, is not constructible as a wire-side vector per §11:173 (wire-side length and base64url-alphabet violations are reported as `E_SCHEMA_FIELD_SYNTAX` at Stage 5, vector 151; the `E_SIG_MALFORMED` diagnostic is reachable only from out-of-band signature decoding, which the corpus does not exercise). The corpus README is updated to document this as a not-vector-constructible code rather than a future tranche.

Seven new vectors:

- **`139-schema-field-length`** - Manifest whose `canary.statement` is 201 bytes (one byte above the §08:112 cap), distinguishing the field-specific Stage 5 cap from the parser-level Stage 3 cap exercised by vector 112. Targets `E_SCHEMA_FIELD_LENGTH`.
- **`156-sig-invalid-key-no-manifest`** - Content document presented without any previously verified manifest, modeling the §11:175 "no relevant verified manifest is available from which to obtain the authorized `runtime_pubkey`" case. Targets `E_SIG_INVALID_KEY`, distinguishing it from `E_SIG_VERIFICATION` (which assumes a key is available and the verify equation fails).
- **`177-origin-invalid-beyond-5y`** - Manifest whose `origin.not_after` is 2031-05-08, more than 5 years past `canary.issued_at` (2026-05-07). Pairs with the existing vector 176 (`not_after_not_later_than_issued_at` reason, renamed from `not_after_not_after_issued_at` under N56) to cover both `reason` values of the `E_ORIGIN_INVALID` structured diagnostic.
- **`178-manifest-updated-future-skew`** - Manifest whose `updated` is 6 minutes ahead of `clock_now`, exceeding the §10 300-second future-skew tolerance. Targets `E_SCHEMA_FIELD_SYNTAX` with `details.reason = "future_beyond_skew_tolerance"` per §10:815, distinct from the Stage 8 canary `issued_at` future-skew check exercised by vector 183.
- **`183-canary-issued-at-future-skew`** - Manifest whose `canary.issued_at` is 6 minutes ahead of `clock_now`, exceeding the §10 300-second future-skew tolerance. Targets `E_CANARY_INVALID` per §08:68,156 ("`issued_at` implausibly in the future"). `updated` is held at `clock_now - 1 minute` so the only live skew violation is the Stage 8 `issued_at` check.
- **`184-canary-runtime-reuse`** - Multi-manifest scenario. A previously verified manifest dated 2026-04-30 (carried as `prior_manifest.json` in extra_files; isolated from the 001 positive fixture to avoid clock-skew confounds) authorizes a runtime_pubkey. The presented manifest at `clock_now` declares the same runtime_pubkey. Targets `E_CANARY_RUNTIME_REUSE` per §08 (rc.19 N55) and §11:200, with structured details `runtime_pubkey`, `previous_issued_at`, `current_issued_at`.
- **`201-migration-chain-cycle`** - Two-manifest scenario realizing the deterministic A -> B -> A chain cycle. The announcing manifest at A carries a `migration_pointer` to successor B (op_pub_2). The successor manifest at B (in extra_files) carries its own `migration_pointer` announcing a return to A. Per §10:436 the per-flow `visited_origins` set forbids re-adopting an address already visited. Targets `E_MIGRATION_INVALID` with structured details `reason = "chain_cycle"`, `announcing_origin_address`, `successor_origin_address`. The diagnostic is deterministic across conforming clients (any client tracking `visited_origins` rejects on the second hop, regardless of chain-depth policy); this isolation is what makes the vector legitimate, in contrast to a hypothetical depth-exceeded vector which would not be deterministic.

The corpus README is updated: the "Categories of vectors" table mentions the new sub-categories; the "Coverage" list removes the now-covered codes (`E_SCHEMA_FIELD_LENGTH`, `E_SIG_INVALID_KEY`, `E_CANARY_RUNTIME_REUSE`, both `E_ORIGIN_INVALID` reasons, `E_SCHEMA_FIELD_SYNTAX` future-skew sub-case, `E_CANARY_INVALID` future-skew sub-case, `E_MIGRATION_INVALID` chain_cycle) and adds a "not vector-constructible" section documenting why `E_SIG_MALFORMED` does not get a vector. The generator at `corpus/tools/generate.py` adds the seven vectors at the end of `negative_vectors`; output remains deterministic byte-for-byte across runs. Total vectors: 49 -> 56. Coverage relative to the §11 error-class catalog rises from 32 to 37 distinct error codes exercised; the additional four sub-cases (the second `E_ORIGIN_INVALID.details.reason`, the manifest-`updated` future-skew specialization of `E_SCHEMA_FIELD_SYNTAX`, the `issued_at` future-skew specialization of `E_CANARY_INVALID`, and the `chain_cycle` `details.reason` of `E_MIGRATION_INVALID`) extend the structured-details coverage of codes that were already exercised via other paths. No protocol, schema, or wire-format change.

**Diagnostic catalog summary.** Seven new error codes from Lotto 11 (all stage 9, all error severity): `E_CONTENT_INDEX_FETCH_FAILED`, `E_CONTENT_INDEX_HASH_MISMATCH`, `E_CONTENT_INDEX_INVALID`, `E_CONTENT_SEQ_MISSING`, `E_CONTENT_SEQ_ROLLBACK`, `E_CONTENT_SEQ_UNCOMMITTED`, `E_CONTENT_HASH_MISMATCH`. Two new diagnostics from Lotto 14: `E_HISTORICAL_NO_PUBLICATION_PROOF` (error, content, stage historical) and `E_CANARY_RUNTIME_REUSE` (error, manifest, stage 8). Lotto 15 adds no new codes; it renames one `details.reason` identifier (N56), revises the trigger list and adds a structured `details` block for `E_MIGRATION_INVALID` (N57), and corrects step ordering in the §08 canary ceremony prose (N58).

**Wire format summary.** Two new optional top-level fields: `content_root` on manifests, `seq` on content documents. One new non-signed resource: `/content_index.json`. One new reserved path. `spec_version` remains `"1.0"` per the rc-cycle additive-field rule (§11 N38). All rc.18 documents validate identically under rc.19 (the new fields are optional and absent in rc.18 documents). Lotto 14 adds no wire-format changes.

**Behavioral compatibility.** N45-N49 (content tree): purely additive and opt-in for publishers. An rc.18 client encountering an rc.19 manifest with `content_root` will reject it as `E_SCHEMA_UNKNOWN_FIELD` (expected closed-schema consequence during RC). An rc.19 client encountering an rc.18 manifest (no `content_root`) operates identically to rc.18. N50 (migration MUST): normative tightening. An rc.18 implementation that did not implement migration history is non-conformant under rc.19. N51 (canary block): behavioral inversion. An rc.18 implementation rendering content on expired canaries without blocking is non-conformant under rc.19 unless it was already in expired-canary-block mode. Permissive-canary mode provides an explicit opt-out. N52-N55 (Lotto 14): normative tightening and new diagnostics. An rc.18 implementation that accepts historical content without publication-existence proof, transmits request-state across K_runtime rotation boundaries, or accepts canaries reusing the same runtime key is non-conformant under rc.19. N56-N58 (Lotto 15): textual errata with no client-conformance impact. N56 renames the `not_after_not_after_issued_at` reason identifier to `not_after_not_later_than_issued_at`; clients that emitted the prior identifier in `E_ORIGIN_INVALID.details.reason` SHOULD update to the corrected identifier but the underlying validation behavior is unchanged. N57 removes a non-deterministic, client-policy-dependent trigger from `E_MIGRATION_INVALID` and aligns the §11 row with §10:442; an rc.18 implementation following §10 is already conformant. N58 corrects a step ordering defect in the §08 canary ceremony prose; the reordering does not eliminate the signature-verification window inherent to single-runtime-key rotation (clients with a cached prior manifest verify against the previous `K_runtime.pub`; clients fetching the new manifest verify against the new `K_runtime.pub`; the publisher signs with one private key at a time, and v1 has no dual-key overlap mechanism) but places the reversible signing-cutover step before the irreversible manifest deploy, so a failure between transferring the new key and deploying the new manifest can be reverted without the anti-downgrade rule (§08:65) trapping the publisher in a state where no served document verifies against the announced runtime key. Publishers SHOULD update their runbook to the corrected ordering and SHOULD perform steps 5 and 6 in immediate succession to minimize the inherent window. No protocol-level rule changes. Lotto 16 (corpus expansion): no protocol or wire-format changes. Implementations conformant under rc.19 protocol semantics will pass the new vectors automatically; implementations that previously short-circuited any of the newly-covered diagnostics into a different code now have explicit corpus-level verdicts to align against.

### v1.0-rc.18 (planned - soak on rc.17 in progress)

This section describes the changes that will land in v1.0-rc.18 when the tag is created. The commit carrying these changes is on `main` ahead of the v1.0-rc.17 tag; rc.18 will be tagged on that commit (or a successor including any soak-period fixes) once the rc.17 soak window concludes without blockers. Until then, this section is forward-looking, not a published release.

Date: TBD (target shortly after rc.17 soak completes)

Changes since v1.0-rc.17 (which equals rc.16 plus the Lotto 6 playbook update and the README updates committed under `docs:` after the rc.16 retag):

This rc lands a textual errata tranche identified as Lotto 7 (ten low-severity clarifications aggregated into a single commit before tagging), a second-pass errata tranche identified as Lotto 8 (four additional low-severity clarifications driven by a fourth-pass audit, landed in a follow-up commit after Lotto 7), a third-pass errata tranche identified as Lotto 9 (four additional clarifications and limitation surfacings driven by a fifth-pass audit: one normative restructuring of the §10 migration successor adoption state, one §11 resolution of a versioning rule contradiction between §11 and the release engineering convention, two new §00 limitation entries surfacing previously-implicit v1.0 gaps), one follow-up clarification (N41) addressing a verified-pending edge case introduced by N37, a fourth-pass errata tranche identified as Lotto 10 (three normative-impact changes driven by a sixth-pass external cryptographic audit: a value-range narrowing on the canary interval, a SHOULD->MUST elevation on First-contact migration confirmation, and an additive submit-validation timing section), and a corpus Phase-1 coverage expansion (15 additional negative vectors filling previously-uncovered §11 diagnostic codes within the existing vector schema). No wire-format change; no schema change; the diagnostic catalog is unchanged. One existing diagnostic `details` field gains a precision constraint. Two SHOULD-level rules are tightened to MUST: the §10 cross-session migration history mitigation gains a recall-window upper bound and a directional fix (Lotto 9 N30/N31), and the §10 First-contact migration confirmation is elevated from SHOULD to MUST (Lotto 10 N43). One value-range MUST narrows: the §08 canary interval ceiling drops from 90 to 30 days (Lotto 10 N42). One §10 normative subsection (Successor verification) is restructured to introduce an explicit verified-pending successor state distinct from adopted; the restructuring makes the existing rc.17 staging of confirmation-gated actions unambiguous but does not add a new MUST that an rc.17 implementation correctly following §10:385 would not already satisfy. One additive §09 subsection (Submit validation timing) provides SHOULD-level guidance on server-side side-channel mitigation for the publisher submit-handling path (Lotto 10 N44).

**§10 Cross-session migration history - Replacement event covers the pre-Adoption current origin (N30)** - In rc.16 the `Replacement` event was recorded only when a previously adopted successor was itself superseded. This left a hole: the cross-session ping-pong vector `A -> B -> A -> B` was detectable in only one direction, because `A` (the original starting origin, never itself a successor before the first Adoption) was never marked as "previously replaced", so a later migration announcing `A` as a successor would not trigger the recall check. The Replacement event is now recorded in two situations: (a) at every Adoption, the pre-Adoption current origin (the announcing origin) is recorded as Replacement; (b) at the existing supersession case. Both directions of the ping-pong are now covered. The Replacement record schema is preserved; only the trigger conditions are extended.

**§10 Cross-session migration history - recall window upper bound (N31)** - The recommended 30-day recall window now has an explicit SHOULD-NOT-exceed of 365 days. Clients with bounded storage MAY enforce a smaller cap, whether by time or by event count (for example, the most recent 100 migration events per publisher profile, evicting the oldest first), provided the cap remains at or above the 7-day floor. Bounds the migration-history storage per publisher profile so that a long-lived `K_publisher.pub` does not accumulate unbounded events.

**§00 v1.0 limitations - publisher history portability (N32)** - Adds an explicit limitation entry stating that Entangled v1 does not specify a serialization format or migration mechanism for publisher history (trust-state pinning records, migration history, abandonment records). A user who switches client implementations resets these records and must re-establish them through PIP-confirmed out-of-band channels. The protocol's identity guarantees still hold across the switch (the new client treats the publisher as First contact); only cross-session continuity benefits are lost. Documents an existing v1.0 design choice rather than introducing one.

**§01 Glossary - `Rendering session` (N11)** - New glossary entry defines a rendering session as the lifetime of a single client commitment to render a specific document instance: it begins after Stages 6 through 9 succeed, ends on user-initiated reload, navigation away, or replacement of the underlying manifest by a newer one. In-flight retry attempts share the same session. Disambiguates the term used in §03 (no-retry rule for image verification failure) and §10.

**§08 Canary - `summary surface` aligned to §10 chrome terminology (N12)** - The §08 phrase "the canary's summary surface" is cross-referenced to §10 "Always-visible compact indicators" so implementations do not interpret the two terms as distinct UI surfaces. The complementary "expandable detail surface" of §10 is cited as the natural location for the contents of `freshness_proof`. No normative change; cross-reference clarification only.

**§10 Abandon retained publisher identity - deletion scope (N13)** - The `delete the active trust-state record for P` step is extended to enumerate what the deletion includes: the TOFU-pinned or Externally verified state itself, the recorded set of authorized origins for the publisher profile, and any pending migration-history events naming `P`. Cached manifests for those origins are explicitly orphaned and evicted by normal cache rules; the client MUST NOT use them as current after the deletion step. Closes the rc.14 ambiguity about what a `K_publisher.pub` abandonment removes from local storage.

**§10 Migration confirmation - `affirmative confirmation` defined (N14)** - The phrase "the user's affirmative confirmation" is now defined explicitly as the user activating a dedicated chrome control whose semantics are unambiguously "accept the migration" - a button, key combination, or equivalent affordance. Passive events (focus changes, mouseover, scroll, dismissal of unrelated UI, navigation away and back, timeout-based auto-acceptance) are explicitly excluded from counting as affirmation.

**§10 Migration confirmation - `trust-state lifetime` defined (N15)** - The phrase "during the same trust-state lifetime" used in the migration re-prompting allowance is now defined: a trust-state lifetime for a publisher identity `P` begins when the client transitions `P` into a retained trust state (TOFU pinning or Externally verified) and ends on (a) user-invoked abandonment, (b) revert to First contact through Changed/mismatch resolution, or (c) user-initiated identity reset. No implicit expiration timer. Re-prompting on the same `(announcing_origin, successor_origin)` pair is now also required when the recall check defined in "Cross-session migration history" names the successor as a previously-replaced address.

**§10 Chain depth and cycle prevention - current origin after cycle rejection (N21)** - When a successor address is rejected as a chain cycle under the `visited_origins` rule, the spec now states explicitly that the publisher profile retains the most recently verified successor adopted earlier in the same flow as the current origin, and that the flow's root origin remains accessible through publisher-history controls. Future fetches addressed at any origin in `visited_origins` MAY use that origin's cached manifest if one is held and is still within its refresh policy; the cycle rejection invalidates the new migration adoption, not the prior verifications.

**§11 `E_ORIGIN_EXPIRED.details.now` - minute-precision rounding (N18)** - The `details.now` field SHOULD be rounded down to minute precision (UTC, RFC 3339 form `YYYY-MM-DDTHH:MM:00Z`). The minute-precision constraint limits the precision of any clock-skew leak when the diagnostic is logged or transmitted to third parties (crash reports, support channels) without compromising the diagnostic's usefulness for clock-skew troubleshooting, where minute-level resolution is sufficient.

**Lotto 8 - second errata tranche (N33-N36)**

A fourth-pass audit pass surfaced four additional low-severity textual issues. They are aggregated into a single follow-up commit on top of Lotto 7. All four are clarifications or consolidations of rules already implied by adjacent text; none alters wire format, schema, diagnostic catalog, or any existing MUST-level requirement.

**§10 and §11 `origin.not_after` boundary wording (N33)** - Three spots described the `origin.not_after` boundary check in inclusive terms inconsistent with the strict normative formula. The §10 Stage 9 pipeline-summary item previously read "reject if the client's clock (within clock-skew tolerance) is at or after the declared instant", the §10 Stage 9 detail prose previously read "a manifest whose declared `not_after` is at or before the client's clock ... is rejected as `E_ORIGIN_EXPIRED`", and the §11 `E_ORIGIN_EXPIRED` catalog row previously read "the client's clock (within clock-skew tolerance) is at or after the declared instant". All three wordings contradicted the §06 phrasing ("strictly later than the declared instant") and the §10 past-bound tolerance formula `current_time > timestamp + 300 seconds` (strict, not inclusive). All three spots are now aligned to strict `>`: the pipeline-summary item cites "strictly later than the declared instant, per the past-bound tolerance formula in 'Clock skew tolerance' below"; the Stage 9 detail prose cites "strictly earlier than the client's clock ... the rejection formula is `current_time > not_after + 300 seconds`, strict"; the §11 catalog row cites "the client's clock is strictly later than the declared instant, applying the past-bound clock-skew tolerance (`current_time > not_after + 300 seconds`)". Textual alignment only. The normative comparison has always been strict `>` (defined at §06 since rc.14 and reinforced by the rc.15 past-bound tolerance formula); only the prose and pseudocode wording in §10 and §11 were inconsistent.

**§05 Small-order `R` not rejected (N34)** - The Ed25519 strict profile in §05 rejects small-order public keys (`A`) but does not reject signatures whose `R` component is a small-order point on the Ed25519 curve, consistent with the `verify_strict` mode of `ed25519-dalek` referenced as the canonical implementation. The §05 "Signature (`R || S`) validation" subsection previously specified only the canonical-encoding check on `R` and was silent on small-order treatment. A clarifying paragraph is added at the end of the subsection, stating explicitly that small-order rejection applies only to `A` and not to `R`, and that a signature with small-order `R` is accepted if it passes canonical encoding, `S < L`, and the cofactorless verification equation `[S]B = R + [k]A`. Textual clarification of existing behavior; no implementation change.

**§07 State update failure taxonomy (N35)** - The §07 "Consent model" subsection previously stated, in adjacent paragraphs, that an undeclared `(namespace, key)` combination causes the transaction document to be rejected, and that "rejecting a state update does not reject the transaction document". The two clauses cover different failure modes (schema/policy hard-fail on the document vs consent/storage soft-fail on the operation only), but the distinction was implicit. The "Consent model" wording is extended with an explicit failure taxonomy enumerating four cases: schema failure and policy failure reject the entire transaction document at Stage 5; consent failure and storage failure reject only the individual state operation, leaving the transaction document valid and renderable. Consolidates rules already present in §02, §07, and §10; no new requirement is introduced.

**§10 Publisher-supplied labels in chrome (N36)** - The §10 chrome rules previously placed the prohibition on publisher-controlled content in chrome ("Chrome separation") adjacent to per-field permissions allowing specific publisher-supplied fields (`manifest.navigation.label`, `meta.title`) to appear in chrome regions, but no single subsection enumerated the permitted fields or imposed display constraints on them as a class. A new subsection "Publisher-supplied labels in chrome" is inserted before "Chrome separation". It enumerates the two publisher-supplied label fields permitted in chrome and imposes three MUST-level requirements on them: display only after Stage 9 verification (already implied by the validation pipeline); visual distinguishability from client-asserted status (consolidating the existing rule that publisher content MUST NOT impersonate client-controlled status); opacity to in-band markup, terminal control sequences (ANSI, VT, OSC, CSI), and other escape mechanisms (extending to chrome the rendering-path opacity already required for state values in §07). Other publisher-supplied fields displayed in chrome (`state.purpose`, proposed state values, `canary.statement`, `canary.freshness_proof`) are governed by safe-display rules in their owning sections, which are referenced from the new subsection. Consolidates existing rules; no new permission to display additional fields in chrome is granted.

**Lotto 9 - third errata tranche (N37-N40)**

A fifth-pass audit pass surfaced four additional issues: a normative restructuring of the §10 migration successor adoption flow, a versioning-rule contradiction between §11 and the release engineering convention, and two v1.0 limitations that were implicit in the spec but not surfaced in §00. They are aggregated into a single follow-up commit on top of Lotto 8.

**§10 Migration successor adoption - verified pending state introduced (N37)** - The §10 "Successor verification" subsection previously stated, in adjacent paragraphs, that the client "adopts the successor origin into the publisher profile" upon successful verification, and that for TOFU pinned and Externally verified trust states the client MUST obtain affirmative user confirmation "before navigating to the successor origin and before migrating any cached state to it". The wording conflated two distinct states - verified but not yet user-confirmed, and adopted as an authorized origin of the publisher profile - and left the publisher-profile transition implicitly happening at verification while the related behaviors were gated on confirmation. The subsection is restructured to introduce a **verified pending successor** state explicitly. Post-verification, the successor is recorded as verified pending in the publisher profile, and the cached successor manifest is current for the successor origin under the standard caching rules (preserving the existing direct-navigation behavior); the successor is not yet an authorized origin of the publisher profile, and the client MUST NOT auto-navigate to it from the announcing origin's session, migrate cached state to it, or render publisher-controlled content from it under that session. Adoption - transition to authorized origin status and recording of the Adoption event in publisher history (rc.18 N30) - occurs at the affirmative user-confirmation step defined in the following subsection, retaining the existing trust-state-dependent strength (MUST for TOFU pinned / Externally verified; SHOULD for First contact, with an explicit relaxation that a client omitting the First contact dialog MAY treat verification as adoption provided the chrome migration notice is displayed). The §10 "Anti-downgrade and anti-forgery interaction" wording at "adopt only on success" is updated to "adopt only on user-confirmation success" for consistency. No new wire format; no new schema; no new diagnostic code; the rc.18 Adoption event mechanism in publisher history is unchanged because Adoption events are recorded at user confirmation, which matches the pre-restructuring intent. An rc.17 implementation that correctly gated auto-navigation, state migration, and content rendering on confirmation per §10:385 already implements the new staging; the restructuring makes the staging explicit and removes the ambiguity around the meaning of "adopts into publisher profile" pre-confirmation.

**§11 Versioning - RC-cycle additive relaxation made explicit (N38)** - §11 "Protocol version" previously stated that "any change to the accepted wire format, including additive fields, is a breaking change at the protocol level. There is no middle ground for additive minor revisions within v1." Read literally, this contradicted the README release-engineering rule (clarified in rc.17) that allows additive optional fields during the pre-release rc cycle without bumping `spec_version`, under which `migration_pointer` (rc.13) and `origin.not_after` (rc.14) were introduced. The two statements are reconciled by adding a paragraph to §11 that distinguishes the wire-format stability guarantee that holds from v1.0 final onward (the no-additive-changes rule) from the pre-freeze rc-cycle relaxation (documented in `docs/RELEASES.md` and explicitly not part of the v1.0 conformance profile). Conformance is judged against v1.0 final; rc intermediate states are not separately conformant artifacts. No normative behavior change; resolves an inconsistency between §11 and the release engineering convention.

**§00 Limitation - content rollback by an authorized runtime key (N39)** - The v1.0 limitations list in §00 did not explicitly enumerate the absence of in-document rollback detection: a server in possession of a previously-issued signed content document for a given `path` can serve that older instance even after the publisher has published a newer instance for the same path, and a client has no protocol-level basis to distinguish stale from current within the same `K_runtime` authorization window. The limitation is implied by the absence of a `revision`/`content_hash` field and is acknowledged as a v2 candidate in `docs/design-decisions.md`, but was not surfaced in §00. A new limitation entry "No detection of content rollback by an authorized runtime key" is added to the §00 list, stating the gap, naming the operator mitigations (rotation of `K_runtime`, publication of a new manifest with a later `canary.issued_at`, out-of-band republication of sensitive updates), and pointing to the v2 candidate.

**§00 Limitation - remembered consent does not reprompt on policy widening within scope (N40)** - Remembered consent under §07 is keyed on `(K_publisher.pub, namespace, key, mode)` and does not include `purpose`, `max_size`, or `max_lifetime`. A publisher can therefore widen the policy for an existing `(namespace, key, mode)` combination (longer `max_lifetime`, larger `max_size`, changed `purpose`) and have subsequent set operations committed under the remembered consent without reprompting the user, provided each new operation stays within the new policy bounds. Existing stored entries retain their commit-time bounds per §07 "Client behavior on policy changes"; the relaxation applies only to new operations after the policy change. The behavior is the spec's intent - tighter binding would be a v1.1+ tightening - but was not surfaced as an explicit limitation in §00. A new limitation entry "Remembered consent does not reprompt on policy widening within the same `(namespace, key, mode)` scope" is added, naming the keying tuple, the relaxation, and the future-tightening candidate status.

**§10 Migration withdrawal - verified-pending case (N41, follow-up to N37)** - N37 introduced the verified-pending successor state, but the §10 "Anti-downgrade and anti-forgery interaction" subsection at line 404 covered only what happens to an *adopted* successor when the announcing manifest is replaced by a newer manifest that omits `migration_pointer`. The case of a verified-pending successor at the time of withdrawal was not explicitly addressed by either the pre-N37 wording or the N37 restructuring. A clarifying sentence is added to the §10:404 paragraph: a verified pending successor that has not yet been adopted is purged from the publisher profile on withdrawal; the cached successor manifest may remain in the manifest cache under the standard caching rules but is no longer associated with a pending migration from the announcing origin, and the verified-pending-state behaviors defined under "Successor verification" no longer apply. Documents the natural semantics; an rc.17 implementation had no verified-pending concept and so could not have implemented this case incorrectly.

**Lotto 10 - fourth errata tranche (N42-N44)**

A sixth-pass audit (external Soatok-style cryptographic review) surfaced three additional issues with normative impact. They are aggregated into a single follow-up commit on top of Lotto 9 / N41. Unlike Lotto 7-9 which were predominantly clarifications and consolidations, Lotto 10 contains two normative tightenings (a value-range narrowing on the canary interval and a SHOULD->MUST elevation on First-contact migration confirmation) and one additive normative section on submit validation timing. All three are bounded in scope; wire format, schema, and the diagnostic catalog remain unchanged.

**§08 Canary interval ceiling - 90 days -> 30 days (N42)** - The canary interval `next_expected - issued_at` previously permitted up to 90 days, with the operator playbook discouraging 60-90 days as "the canary stops being a meaningful security signal" and identifying 30 days as the upper end of the medium-threat band. The §08:81 MUST ceiling is now tightened from 90 days (7776000 seconds) to 30 days (2592000 seconds), aligning the protocol-level MUST with the operational upper bound previously recommended by the playbook. The 7-day MUST floor is unchanged. The §08:88 prose is rewritten: higher-threat publishers SHOULD choose intervals close to 7 days; lower-threat publishers MAY choose intervals up to the 30-day ceiling. The operator playbook §7 "Choosing canary interval" is restructured: the "Low-threat - 30 to 60 days" and "Discouraged - 60 to 90 days" buckets are removed; the remaining buckets are "High-threat - 7 days" and "Standard - 14 to 30 days". References to the 90-day cap in operator-playbook §3.3 and §16 "The compromise window" are updated to 30 days. The §08:154 Invalid state description ("7-to-90-day bounds") is updated to "7-to-30-day bounds". The change is a normative tightening: an rc.17 publisher emitting a canary interval in `(30, 90]` days is non-conformant under rc.18. The operator playbook already discouraged the 60-90 days portion of this range; the 31-60 days portion is the genuinely new restriction. Wire format and schema are unchanged; only the accepted value range narrows.

**§10 First contact migration confirmation - SHOULD -> MUST (N43)** - The §10:390 user-confirmation requirement for migration adoption at First contact was previously SHOULD-level, with rationale that "the migration-confirmation dialog is informational rather than a continuity-preservation step". An external audit observed that this permits silent auto-adoption of a successor at First contact, opening a vector under temporary `K_publisher` compromise (a compromise window can publish a migration announcement that is then auto-adopted by every First-contact client without prompting) and a cross-session ping-pong vector where the same First-contact identity re-traverses an `A -> B -> A -> B` chain on each fresh navigation without user awareness. The §10:390 SHOULD is tightened to MUST under the same definition of "affirmative confirmation" as for Externally verified and TOFU pinned states. Cascading updates: the §10:383 "SHOULD-level relaxation of that subsection permits a client that omits the confirmation dialog to treat verification as adoption" sentence (introduced by Lotto 9 / N37) is removed; the user-confirmation step is now MUST for all announcing trust states (Externally verified, TOFU pinned, First contact), and a verified pending successor never transitions to adopted without it. The §10:429 "For First-contact identities, where one automatic migration hop is permitted without a user-confirmation prompt under 'User confirmation' above, this re-traversal can be silent" wording is updated: the prompt is now always present, and the residual cross-session concern shifts from "silent re-traversal" to "uninformative re-traversal prompt" (the per-flow `visited_origins` does not surface the cycle without cross-session history; the cross-session history mitigation remains SHOULD per Limitation #3). The §10:443 recall-window text is updated since First contact already requires confirmation; the recall-information surfacing requirement remains SHOULD. The change is a normative tightening: an rc.17 implementation that auto-adopted a migration successor at First contact without prompting is non-conformant under rc.18. The cost is one additional prompt on First-contact navigations that encounter a `migration_pointer`; the benefit is closure of the silent-adoption vector.

**§09 Submit validation timing - server-side side-channel guidance (N44)** - The §09 transport section previously had no normative guidance on submit validation timing. The natural sequential publisher-side validation path (JSON parse, JCS canonicalization, schema check, `request_state` policy check, `request_hash` computation) exposes the rejecting stage as a server-side timing signal that an attacker probing with crafted submit bodies can sample without authentication. The §00:170 Limitation #12 acknowledges the analogous *client-side* concern (validation pipeline diagnostic-stage emission) but the *server-side* mirror was unaddressed. A new §09 subsection "Submit validation timing" is inserted between "Submit body size limit" and "Image resource fetches". Publishers SHOULD adopt one of two disciplines: complete-validation (run all stages regardless of earlier failures, accumulating any rejection reason for the final response without short-circuiting), or randomized-delay (50-200 ms uniformly distributed per request before responding, independent of validation outcome). The rule is SHOULD-level because the protocol cannot enforce timing properties on remote endpoints; operators of high-threat deployments are expected to verify their stack against the discipline. The client-side concern remains acknowledged as Limitation #12 and is not addressed by this rule. Additive normative section; no impact on client conformance and no wire-format change. An rc.17 publisher implementation is not retroactively non-conformant because the rule is SHOULD-level guidance newly added; rc.18+ publishers operating in high-threat deployments are expected to align.

**Diagnostic catalog summary.** No new error codes. One existing `details` field gains a precision constraint: `E_ORIGIN_EXPIRED.details.now` rounds down to minute precision. The rc.16 `details` schema is described as `SHOULD include`, so the precision constraint does not break any conforming rc.16 implementation that already emits second-precision values; it tightens future implementations.

**Wire format summary.** Unchanged. The wire-level `spec_version` remains `"1.0"`. No schema additions or removals on any document kind. JCS canonicalization, NFC, byte caps, and all other input restrictions are unchanged. All rc.17 documents validate identically under rc.18.

**Behavioral compatibility.** Lotto 7-9 changes are clarifications, additive extensions, or definitional tightenings of existing rules. The `Replacement` event extension (N30) changes the set of recorded events under the SHOULD-level migration-history mechanism but does not change any MUST-level requirement; rc.16 implementations that do not maintain migration history at all remain fully conformant. The recall-window upper bound (N31) is a SHOULD-NOT-exceed; rc.16 implementations that chose recall windows <= 365 days remain conformant by default. The §00 portability note (N32) is a limitation, not a requirement. The §10 abandon, affirmative-confirmation, trust-state-lifetime, and current-origin clarifications (N13, N14, N15, N21) define previously-undefined terms with the meaning the spec already implied; no previously valid behavior is now invalid. The `details.now` precision constraint (N18) tightens an optional `details` field; rc.16 emitters of second-precision values become non-conformant only against the SHOULD-level guidance, not the MUST-level diagnostic structure. The Lotto 8 entries (N33-N36) are clarifications or consolidations: N33 is a pseudocode wording alignment with the existing normative formula; N34 is a textual clarification of `verify_strict` semantics that all conforming rc.17 implementations already implement; N35 is a taxonomic consolidation of failure modes whose semantics are already specified across §02, §07, and §10; N36 introduces three new MUST clauses on publisher-supplied chrome labels, all of which are operationally satisfied by any rc.17 implementation that respects the schema-level character-class restrictions on `manifest.navigation.label` and `meta.title` (both fields already prohibit C0 controls including `ESC` U+001B, which precludes ANSI/VT escape sequences in their content) and the existing chrome separation rule. The Lotto 9 entries (N37-N40) are also clarifications or consolidations: N37 introduces the verified-pending successor state and tightens the staging of confirmation-gated actions, but an rc.17 implementation that correctly followed §10:385 (no auto-navigation or state migration before confirmation for TOFU pinned / Externally verified) already implements the new staging; N38 resolves a textual contradiction between §11 and the release engineering convention without changing wire-format behavior; N39 and N40 are new entries in the §00 limitations list documenting existing behavior, not new requirements.

The Lotto 10 entries (N42-N44) include two genuine normative tightenings and one additive SHOULD-level section. N42 narrows the §08:81 canary interval ceiling from 90 to 30 days: an rc.17 publisher emitting a canary interval in `(30, 90]` days is non-conformant under rc.18. The 60-90 days portion of this range was already discouraged by the operator playbook (Lotto 6 cadence floors recommended <= 30 days for the medium-threat band and identified 60-90 days as "discouraged"); the 31-60 days portion is the genuine new restriction. No known rc.17 implementation operates above 30 days in practice. N43 elevates the §10:390 First-contact migration confirmation from SHOULD to MUST: an rc.17 client that auto-adopted a migration successor at First contact without a confirmation prompt is non-conformant under rc.18. The cascading edits in §10:383, §10:429, and §10:443 are textual alignments with the new MUST and do not introduce additional independent requirements. N44 adds the §09 "Submit validation timing" subsection as SHOULD-level publisher guidance; it does not affect client conformance and does not change any existing publisher MUST. An rc.17 publisher is not retroactively non-conformant on N44; rc.18+ publishers in high-threat deployments are expected to align with one of the two disciplines (complete-validation or randomized-delay). The Lotto 10 tightenings (N42, N43) bring the protocol-level MUST surface in line with what the operator playbook and external audit feedback had already identified as the operationally meaningful boundaries.

**Corpus.** `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.16"` to `"1.0-rc.18"` (rc.17 was content-identical to rc.16; no separate corpus snapshot is needed for rc.17). Total vectors grow from 34 to 49 under the Phase-1 coverage expansion: 15 new negative vectors fill previously-uncovered §11 diagnostic codes within the existing vector schema. Newly covered codes: `E_INPUT_BYTE_CAP`, `E_PARSE_JSON`, `E_PARSE_NESTING_DEPTH`, `E_PARSE_STRING_LENGTH`, `E_PARSE_ARRAY_LENGTH`, `E_PARSE_OBJECT_KEYS`, `E_KIND_MISSING_FIELDS`, `E_SCHEMA_FIELD_TYPE`, `E_SCHEMA_FIELD_RANGE`, `E_SCHEMA_BLOCK_NOT_PERMITTED`, `E_SCHEMA_DUPLICATE_ENTRY`, `E_SCHEMA_MALFORMED_UNICODE`, `E_BIND_ORIGIN`, `E_ORIGIN_INVALID`, `E_CANARY_INVALID`. Coverage relative to the §11 catalog rises from 22 to 32 distinct codes; remaining uncovered codes (Stage 1 transport `E_TRANSPORT_*`, Stage 7 trust `E_TRUST_*`, warning-class `W_CANARY_*` / `W_IMAGE_*` / `W_HISTORICAL_*`, `E_STATE_*`, `E_HISTORICAL_*`, and a small set of binding/migration sub-codes) require vector-schema extensions (HTTP response metadata, image-fetch fixtures, an `expected.warnings` array, multi-manifest history) and are deferred to a future tranche. The 34 rc.16 vectors are byte-for-byte identical in the rc.18 corpus; no existing diagnostic codes, schemas, or expected verdicts change. `corpus/README.md` is updated: the categories table is extended to list each new vector ID range, and the "future tranches" paragraph is replaced with an explicit enumeration of uncovered §11 codes and the vector-schema extensions each would require.

### v1.0-rc.17

Date: 2026-05-11

Changes since v1.0-rc.16:

This rc adds no normative protocol changes. It bundles the post-rc.16 documentation tranches (Lotto 6 operator-playbook operationalization of W2, plus the README updates that brought the high-level docs into alignment with rc.13-rc.16 wire-format additions) into a single tagged snapshot suitable as a v1.0 release candidate. The wire format, schemas, diagnostic catalog, and corpus are byte-for-byte identical to rc.16; an implementation conforming to rc.16 conforms to rc.17 without modification.

**Lotto 6 - operationalization of W2 in the operator playbook (`docs/operator-playbook.md`)** - The first-pass audit identified W2 as a structural ALTA limitation: Entangled v1 has no in-band cryptographic revocation of `K_runtime`, so a compromised runtime key authorizes content forgery for the duration of the canary window plus the post-rotation residual. The protocol-level closure of W2 is deferred to a future major (v1.1 or v2) where a `runtime_revocation` mechanism with clock-anchor semantics can be designed cleanly. In the meantime, the playbook is extended in three places to make the residual exposure concrete and the response procedure actionable:

* §3.3 "K_runtime" - quantifies the maximum compromise window: canary interval (<=90 days per §08) plus the §10 300-second clock-skew tolerance.
* §7 "Canary and runtime rotation ceremony" - adds a new "Choosing canary interval" subsection with cadence floors per threat profile: high-threat (journalism with sensitive sources, financial services) 7 days; medium-threat 14-30 days; low-threat 30-60 days; 60-90 days discouraged because the canary stops being a meaningful security signal.
* §16 "Runtime key compromise response" - substantially expanded from a flat 8-step procedure to: a "Compromise window" subsection decomposing the exposure into three temporal phases (pre-discovery, pre-rotation, post-rotation residual) each with quantitative bounds; expanded steps that prescribe immediate-rotation cadence (`next_expected = 7 days` even on deployments normally using longer intervals); a new "Out-of-band announcement" subsection with a complete announcement template covering publisher PIP, compromised and replacement `K_runtime.pub`, compromise window in UTC, affected canary `issued_at`, reader instructions, and incident description; a new "Reader guidance" subsection with explicit safe/suspect/action/non-action items; and an expanded "Limitation" subsection listing the concrete consequences of the missing in-band revocation and pointing to the v1.1+ roadmap.

The playbook is non-normative; these changes do not affect protocol conformance. They do shift the operator's posture from "rotate when you remember" to "rotate by threat profile, with prepared offline ceremony state and PIP-confirmed out-of-band channels actively maintained".

**README updates** - The post-rc.16 README pass corrected drift accumulated during the additive rc cycle.

* `README.md` versioning section: the prior text "any change to the accepted wire format, including additive fields, is a breaking protocol change and requires a new protocol version" was contradicted by what we actually did in rc.13 (added `migration_pointer`) and rc.14 (added `origin.not_after`) without bumping `spec_version`. The text is reformulated to clarify that during the pre-release rc cycle, the closed schema MAY be extended additively with optional fields without bumping `spec_version`; documents valid under an earlier rc remain valid under a later rc. The freeze rule applies once v1.0 is tagged final.
* `README.md` "Identity continuity": adds a paragraph citing `migration_pointer` as the in-band mechanism for origin rotation, with reference to chain-depth and per-flow cycle prevention. The high-level overview now mentions the rc.13-rc.14 mechanisms explicitly.
* `corpus/README.md` vector ID category: extended from "100-199 negative" to "100-199 single-document negative diagnostics, 200-299 multi-document scenarios such as migration", to accommodate vector 200 (the migration successor-origin-expired vector added at rc.16).
* `corpus/README.md` vector schema: documents `expected.diagnostic_details` (added at rc.16 for `E_MIGRATION_MISMATCH` with structured `details`) and clarifies that for multi-document scenarios the verdict refers to the scenario outcome, not necessarily the in-isolation validity of the primary document.
* `corpus/README.md` test keys: documents `origin_2` (the second origin keypair added at rc.16 for migration scenarios), alongside the existing `runtime_2`.
* `corpus/README.md` categories table: adds rows for `190-199` (Unicode/canonicalization, where vector 190 had been emitted since rc.13 but unindexed) and `200-209` (migration scenarios). "Future tranches" list cleaned up to reflect what is now covered.

**Wire format summary.** Unchanged. `spec_version` remains `"1.0"`. All rc.16 documents validate identically under rc.17. The corpus index `rc_target` was not bumped at the rc.17 commit because the corpus is content-identical to rc.16; the next bump occurs at rc.18 where the Lotto 7 errata land.

**Behavioral compatibility.** rc.17 introduces no new requirements on clients or publishers. rc.16 conformance implies rc.17 conformance.

**Tag.** `v1.0-rc.17` was placed at the commit that landed the README updates (`62956f9`), one commit above `2a2fbea` (Lotto 6 playbook). Both commits are included in the tag's tree.

### v1.0-rc.16

Date: 2026-05-11

Changes since v1.0-rc.15:

This rc lands a small follow-up tranche identified as Lotto 5: one rename for clarity, one new SHOULD-level mitigation for cross-session migration cycles, and the first corpus coverage of `origin.not_after` and `migration_pointer`. No wire-format change; no schema change; one diagnostic-catalog field is renamed (additive; rc.15 consumers see the previous name as absent).

**§11 `E_MIGRATION_MISMATCH.details.underlying_diagnostic` renamed to `underlying_diagnostic_code` (N22)** - The rc.15 field name `underlying_diagnostic` was ambiguous about scope: read literally it could be either "the diagnostic code identifier" (a string) or "the structured diagnostic record" (an object with `code`, `stage`, `details`). The intent was the former. The field is renamed to `underlying_diagnostic_code` and its definition explicitly states "code identifier only, encoded as a string; not the full structured diagnostic record". The successor's own `details` object is not nested. An operator wishing to inspect the successor's full diagnostic record fetches the successor manifest in isolation and observes the diagnostic produced by the standard pipeline. The §10 "Successor verification" prose is updated to use the new name. Rc.15 consumers reading the `details` of an rc.16 `E_MIGRATION_MISMATCH` see the rc.15 `underlying_diagnostic` key as absent and the rc.16 `underlying_diagnostic_code` key as unrecognized; the diagnostic semantics (migration rejected) are unchanged. This is a clarification rename, not a functional change; the rc.15 wire-level diagnostic schema is described as `SHOULD include`, so rc.15 implementations were never required to emit `underlying_diagnostic` in the first place.

**§10 cross-session migration history (N20)** - Adds a new SHOULD-level subsection "Cross-session migration history" under "Origin migration" that mitigates the cross-session ping-pong vector identified in the third-pass audit (an `A -> B` migration in one session followed by `B -> A` in the next, forced on every fresh navigation by an empty per-flow `visited_origins`). The subsection defines two new event classes recorded in publisher history per `K_publisher.pub`: **adoption** (a successor address adopted into the publisher profile) and **replacement** (a previously adopted successor superseded by a newer migration announcement). Clients that maintain migration history SHOULD consult publisher history when processing a new migration to a successor address, looking for prior replacement of that address within a recall window. The recommended recall window is 30 days; configurable, with a 7-day minimum and a discouraged-but-permitted zero (no recall). When a successor is in the recall window as a previously-replaced address, the client SHOULD raise friction: for **Externally verified** and **TOFU pinned** the existing user-confirmation requirement applies but the dialog SHOULD surface the recall information; for **First contact**, where automatic adoption of one hop is otherwise permitted, the client SHOULD instead require explicit user confirmation. The mitigation is SHOULD-level rather than MUST because v1 does not specify a publisher-history storage backend beyond what is required for trust-state continuity, and migration history introduces a new event class within publisher history. Implementations that do not maintain migration history are not non-conformant; the limit is documented as a v1.0 limitation in §00. `visited_origins` (per-flow, MUST) and migration history (per-publisher, SHOULD) are independent mitigations: the former rejects intra-flow cycles outright as `E_MIGRATION_INVALID`; the latter raises friction on cross-session cycles without rejecting them, since a publisher rotating between addresses for legitimate operational reasons must remain reachable. New §00 limitation entry "Cross-session migration cycle mitigation is opt-in"; new §01 glossary entry "Migration history".

**Corpus - first coverage of `origin.not_after` and `migration_pointer` (N23)** - The corpus generator gains support for the rc.14 `origin.not_after` and `migration_pointer` fields on `make_manifest`, the `vec()` helper gains a `diagnostic_details` parameter for structured diagnostic outcomes, and a second origin keypair (`ORIGIN_SEED_2` / `origin_pub_2`) is added for migration scenarios. Two new vectors are emitted: **006-manifest-valid-not-after** (positive) - minimal valid manifest declaring `origin.not_after = 2027-05-07T00:00:00Z`, exercising rc.14 schema acceptance and Stage 5 cross-field semantic checks per the rc.15 Stage 5 clarification; **200-migration-successor-origin-expired** (negative, migration outcome) - announcing manifest with `migration_pointer` to a successor whose own `origin.not_after` has passed at `clock_now` (2026-05-07). The successor manifest is in `extra_files/successor_manifest.json`. Expected outcome: `E_MIGRATION_MISMATCH` with `details.mismatch_field = "successor_stage9_failure"` and `details.underlying_diagnostic_code = "E_ORIGIN_EXPIRED"` - first vector exercising the rc.15 `successor_stage9_failure` path. The reject verdict on vector 200 refers to the migration adoption outcome; the announcing manifest is itself accepted at its origin, which is implied by the vector's structure. Total vectors: 34 (was 32). All previously emitted vectors are byte-for-byte unchanged.

**Diagnostic catalog summary.** No new error codes. One existing `details` field renamed: `E_MIGRATION_MISMATCH.details.underlying_diagnostic` -> `underlying_diagnostic_code` (N22). The rc.15 `details` schema is described as `SHOULD include`, so the rename does not break any conforming rc.15 implementation - rc.15 emitters never depended on the field appearing under a specific name in their own output.

**Wire format summary.** Unchanged. The wire-level `spec_version` remains `"1.0"`. No schema additions or removals on any document kind. JCS canonicalization, NFC, byte caps, and all other input restrictions are unchanged. All rc.15 documents validate identically under rc.16. The new corpus vectors exercise schema constructs (`origin.not_after`, `migration_pointer`) that were already permitted at rc.14 but not previously covered by the corpus.

**Behavioral compatibility.** N22 is a clarification rename of an optional diagnostic field; no behavior change. N20 adds a SHOULD-level mitigation; clients that ignored N20 in rc.15 (which all do, since N20 is new in rc.16) remain conformant. N23 is corpus-only, no normative spec change. No previously valid behavior is now invalid.

**Deferred - partially closed.** N20 is now addressed at SHOULD level rather than fully closed. The MUST-level promotion would require v1 to prescribe a publisher-history storage backend, which is intentionally left unspecified in v1.0. A future major (v1.1 or v2) may revisit. The limit is documented in §00.

**Corpus.** `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.15"` to `"1.0-rc.16"`. Total vectors 34 (32 -> 34). The 32 prior vectors are byte-for-byte identical between rc.15 and rc.16 corpora.

### v1.0-rc.15

Date: 2026-05-11

Changes since v1.0-rc.14:

This rc lands a small textual hardening tranche identified as Lotto 4: three patch-level fixes addressing internal inconsistencies and a normative gap in the rc.14 text. No wire-format change; no schema change; the diagnostic catalog is extended with two additional `details` fields on an existing error code.

**§10 Stage 5 - cross-field semantic checks (N16)** - The Stage 5 closed-schema validation list is extended with a sixth bullet acknowledging cross-field semantic checks declared by the owning section, including temporal-relation constraints between two fields. The §06 `origin.not_after` constraints (`not_after` strictly later than `canary.issued_at`; not more than 5 years after `canary.issued_at`) are explicitly cited as an instance of this category. Without this addition, the Stage 5 definition listed only per-field and nested-object/array checks, leaving a textual ambiguity about whether the two cross-field arithmetic constraints on `not_after` belonged at Stage 5 (where rc.14 assigns them under `E_ORIGIN_INVALID`) or somewhere later in the pipeline. The §10 prose at "Stage 9: path and origin binding" is also tightened to cite "Stage 5 definition above" when explaining where the `E_ORIGIN_INVALID` rejection occurs.

**§10 Clock skew tolerance - past-bound checks and broken cross-reference (N17)** - Two issues are addressed. First, a textual cross-reference at the §10 Stage 9 prose pointing to a section called "Clock and tolerance below" is corrected to "Clock skew tolerance below"; the section it intended to reference has always been titled "Clock skew tolerance". Second, the "Clock skew tolerance" section is extended with a symmetric past-bound case: the same 300-second tolerance that applies to future-bound checks (`manifest.updated`, `canary.issued_at`) now applies symmetrically to past-bound expiration checks, with `origin.not_after` (§06) as the only field in v1 to which it applies. The rejection formula is `current_time > timestamp + 300 seconds`. The user-presentation hint about local-clock incorrectness is extended to cover `E_ORIGIN_EXPIRED` as well as the previously listed diagnostics. The 300-second tolerance buys the publisher a margin to publish a successor manifest near the declared instant without producing a brief window of false-positive rejection by clients with slightly fast clocks.

**§11 `E_MIGRATION_MISMATCH` details - preserve underlying Stage 9 cause (N19)** - The `E_MIGRATION_MISMATCH` `details` schema is extended in two ways. The `mismatch_field` enumeration gains a fifth value, `successor_stage9_failure`, used when the successor manifest fails any Stage 1 through 9 check independently of the migration-binding fields (publisher pubkey, address, origin pubkey). A new optional field `underlying_diagnostic`, present only when `mismatch_field` is `successor_stage9_failure`, carries the structured diagnostic code that the successor manifest's pipeline would have reported in isolation (for example, `E_ORIGIN_EXPIRED` for a successor whose own `origin.not_after` has passed, `E_SIG_VERIFICATION` for a successor signature failure, or `E_TRUST_MISMATCH` for a successor whose retained identity already conflicts with the announced publisher key). The migration is rejected under `E_MIGRATION_MISMATCH` regardless of the underlying cause; the new field is informational only, intended to give publisher operators enough signal to distinguish "the successor address is wired to the wrong key" from "the successor manifest itself is broken" without requiring out-of-band diagnostic lookup. The `successor_publisher_pubkey` field, when included, is now scoped to cases where the successor's own Stage 5 schema validation succeeded; for earlier-stage failures it is omitted. The §10 "Origin migration" subsection prose at "Successor verification" is updated to reference the new `details.mismatch_field` and `details.underlying_diagnostic` semantics.

**Diagnostic catalog summary.** No new error codes. One existing code's `details` schema is extended additively: `E_MIGRATION_MISMATCH` gains the `successor_stage9_failure` value on `mismatch_field` and the optional `underlying_diagnostic` field. Implementations consuming the rc.14 diagnostic format remain forward-compatible: rc.14 consumers that hardcode the previous `mismatch_field` enumeration will see an unrecognized value when an rc.15 implementation reports `successor_stage9_failure`, but the diagnostic semantics (migration rejected) are unchanged.

**Wire format summary.** Unchanged. The wire-level `spec_version` remains `"1.0"`. No schema additions or removals on any document kind. JCS canonicalization, NFC, byte caps, and all other input restrictions are unchanged. All rc.14 documents validate identically under rc.15.

**Behavioral compatibility.** All rc.15 changes are clarifications and additive extensions of existing diagnostic data. No previously valid behavior is now invalid. Conforming rc.14 clients are not made non-conformant by rc.15; rc.15 clients reporting the new `details` values are not non-conformant under rc.14 because the rc.14 diagnostic schemas are described as `SHOULD include`, not `MUST include exactly`.

**Deferred.** N20 (cross-session migration cycles, forced re-migration, and the persistent migration history that would mitigate them) was identified in the third-pass audit as a MEDIA-severity finding but requires a design decision about how to record migration history persistently and how to set the recall TTL. It is not included in this rc and remains tracked for future work, possibly v1.1.

**Corpus.** `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.14"` to `"1.0-rc.15"`. Total vectors unchanged at 32; the existing vectors are unchanged byte-for-byte and validate identically under rc.15 since all rc.15 changes are textual or additive on diagnostic `details` fields, and no existing vector exercises `E_MIGRATION_MISMATCH` with a successor Stage 9 failure path.

### v1.0-rc.14

Date: 2026-05-10

Changes since v1.0-rc.13:

This rc lands the audit-driven hardening tranche identified as Lotto 3, split into two batches: tightening of `MAY`/`SHOULD` to `MUST` and clarification of underspecified resolution flows (Lotto 1, patch-level, no schema change), and additive features for origin lifetime declaration and migration chain control (Lotto 2, additive, schema and diagnostic-catalog changes).

**Lotto 1 - patch-level tightening (§02, §03, §08, §09, §10)**

- §02 Closed-schema eager-validation MUST (W16) - New normative sentence. Implementations MUST eagerly validate every top-level field of a document against the closed schema. A parser that examines only the fields it intends to use, and ignores or defers fields it does not recognize or does not currently consume, is non-conformant: schema validation is a whole-document check at Stage 5, regardless of which fields a particular execution path reads.
- §03 No retry on image verification failure (W28) - New normative subsection. After a failure at any of steps 2-9 of the image verification pipeline (transport failure, Content-Type or media-type mismatch, oversize, hash mismatch, decode failure, dimension mismatch, pixel-budget violation), the client MUST NOT re-fetch the same `src` for the same `image` block within the same document rendering session. The bound triple `(src, sha256, media_type)` is declared by the signed document; refetching cannot change the signed expectation. A user-initiated reload, or a document fetched anew under a refreshed manifest, is a new rendering session and is not blocked.
- §08 `freshness_proof` summary visibility (N4) - Strengthened the existing presence/absence signaling rule. The presence-or-absence signal MUST be visible in the canary's summary surface - the surface that exposes the canary state without requiring expansion of a detail view, drawer, or other collapsed UI. A client that hides the presence signal exclusively behind a collapsed detail surface is non-conformant. The contents of `freshness_proof` itself, when present, MAY remain in the expandable detail surface; only the presence-or-absence indicator is required in the summary.
- §08 Canary-conflict resolution control (N2) - Strengthened the existing canary-conflict warning rules. The client MUST present an explicit user-accessible resolution control as part of the chrome warning, offering at least two distinct actions: "Keep the retained identity" (acknowledges the conflict, clears the always-visible warning, records the event in publisher history) and "Abandon the retained publisher identity" (defined in §10). Passive events MUST NOT clear the warning. The prior text required offering "the option to abandon" but did not prescribe that the offer take the form of a user-accessible UI control; clients that surfaced the warning without a usable resolution affordance left the user in a permanently bloccato state.
- §09 No `Range`/`If-Range` request headers; no `206 Partial Content` (W26) - Added `Range` and `If-Range` to the prohibited request-header list for both `GET` and `POST` requests, with a normative paragraph explaining the rationale (an Entangled document is signed as a whole and is verified, hashed for the byte cap, and digested for image binding against its complete byte sequence; partial-content fetches deliver bytes that do not correspond to any signed object). Publishers MUST treat any request carrying `Range`/`If-Range` as malformed; publishers MUST NOT emit `206 Partial Content`. The general "unlisted `2xx`" rule already covered `206` on the client side, but the previous text did not name it explicitly and did not forbid the client from issuing `Range` requests.
- §10 Abandoning a retained publisher identity (N1) - New normative subsection specifying the semantics of the "abandon retained publisher identity" action offered by the canary-conflict resolution control (§08), the Changed/mismatch resolution flow, and any equivalent high-threat-mode UI. The action MUST: delete the active trust-state record for `K_publisher.pub = P`; record the abandonment in publisher history as a terminal event including reason class; dissociate any authorized origins (including successor origins adopted under the publisher profile via `migration_pointer`); and refuse to use retained `K_runtime` authorization-history entries for verifying any new content fetched after the abandonment as if it were current publication. After abandonment, a subsequent navigation to any origin presenting `publisher_pubkey == P` is treated as First contact; the client MUST surface that an abandonment record exists at the First-contact prompt. The new subsection also distinguishes "abandon the site" (Changed/mismatch resolution; preserves the retained identity record) from "abandon retained publisher identity" (severs the trust relationship); the two are distinct user actions and MUST be presented in chrome as distinct choices.
- §10 Migration confirmation for retained identities (N5) - Strengthened the prior `SHOULD` on user confirmation before automatic navigation to a successor origin. The strength now depends on the announcing publisher's trust state: for **Externally verified** or **TOFU pinned** identities, the client MUST obtain the user's affirmative confirmation before navigating to the successor origin and before migrating any cached state; for **First contact**, the prior `SHOULD` is retained. The confirmation dialog MUST present both addresses, the complete 24-word PIP, and the `migration_pointer.announced_at` timestamp, with no destructive default. A client that has obtained confirmation for a specific `(announcing, successor)` pair MAY proceed without re-prompting for the same pair within the same trust-state lifetime. Background auto-fetch of the successor manifest for verification purposes remains permitted, provided publisher-controlled content from the successor is not rendered before user confirmation.

**Lotto 2 - additive (§05, §06, §10, §11, §00, §01)**

- §06 `origin.not_after` (W1) - New OPTIONAL field on the manifest's `origin` object. Declares the UTC instant after which the publisher commits that this carrier endpoint is no longer authoritative for the site under `K_publisher`. Encoded in the same RFC 3339 UTC form as `updated` (`YYYY-MM-DDTHH:MM:SSZ`). Constraints: MUST be strictly later than `canary.issued_at`; SHOULD be strictly later than `canary.next_expected`; MUST NOT be more than 5 years after `canary.issued_at`. A manifest with `origin.not_after` violating these constraints is rejected at Stage 5 as `E_ORIGIN_INVALID`. A manifest whose `origin.not_after` is at or before the client's clock (within the §10 clock-skew tolerance) is rejected at Stage 9 as `E_ORIGIN_EXPIRED`; anti-downgrade still applies, and a newer manifest for the same `K_publisher.pub` supersedes the expired one. Absent in the manifest when no expiration is declared (per §04 no-`null` discipline). Bounds the time window during which an attacker holding a compromised `K_origin_priv` can serve cached clients of an abandoned origin.
- §05 `K_origin` compromise summary - Extended the recovery paragraph to acknowledge `origin.not_after` as the optional mechanism that bounds the post-compromise serving window for cached clients; recovery remains in-band only insofar as the cached client observes the new manifest or its `migration_pointer` before `not_after` is reached.
- §10 Origin migration chain depth and cycle prevention (N6) - New normative subsection. A client supporting publisher profiles MUST enforce both an automatic chain-depth limit (at most one `migration_pointer` hop adopted without re-prompting under "User confirmation"; further hops require a new confirmation, evaluated under the trust state of the publisher identity at the intermediate origin) and a per-flow visited-origin set (`visited_origins`, initialized with the announcing origin; a successor address already in `visited_origins` is rejected as `E_MIGRATION_INVALID` with `details.reason = "chain_cycle"`). The `visited_origins` set is per-navigation and per-publisher-profile, reset when the migration-resolution flow ends, and not persisted across sessions. The chain-depth limit applies to automatic hops only; user-confirmed hops reset the counter. High-threat mode MAY set the automatic limit to zero. When the limit is reached without user confirmation, the deeper successor is treated as "pending user action": chrome displays the announcement, but the client MUST NOT auto-navigate, fetch publisher-controlled content from the deeper successor, or migrate cached state to it.
- §10 Stage 9 - Updated the Stage 9 pipeline-summary listing to include the `origin.not_after` check and the `migration_pointer` successor/chain-depth check. Updated the Stage 9 detail prose to specify that `origin.not_after` is evaluated after carrier origin binding succeeds and that the comparison uses the §10 clock-skew tolerance. Cross-references between §06, §10, and §11 are kept consistent.
- §11 - Added `E_ORIGIN_EXPIRED` (Stage 9, manifest, error) and `E_ORIGIN_INVALID` (Stage 9, manifest, error) to the Binding diagnostics catalog. `E_ORIGIN_EXPIRED` SHOULD include in `details` the declared `not_after` and the client's `now` value used for the comparison. `E_ORIGIN_INVALID` SHOULD include `reason` (`not_after_not_after_issued_at` or `not_after_beyond_5y`), the declared `not_after`, and `canary.issued_at`. Extended the existing `E_MIGRATION_INVALID` row to note that the chain-depth/cycle case also reports under this code. [Historical note: the `not_after_not_after_issued_at` identifier was corrected to `not_after_not_later_than_issued_at` under Lotto 15 N56; the `E_MIGRATION_INVALID` chain-depth/cycle row was swapped to drop chain-depth and document `chain_cycle` explicitly under Lotto 15 N57.]
- §00 - Added a new v1.0 limitation entry, "No in-band revocation of `K_origin`", noting that `origin.not_after` is the optional protocol-level bound on post-compromise serving window and that, when omitted, the recovery window is unbounded for cached clients that never observe the new manifest or `migration_pointer`.
- §01 - Added "Origin not-after" glossary entry; updated "Origin" entry to list `not_after` among origin fields; updated "Migration pointer" entry to note the chain-depth limit and visited-origin cycle check.

**Diagnostic catalog summary.** Two new error codes: `E_ORIGIN_EXPIRED`, `E_ORIGIN_INVALID`. One existing code's row text extended: `E_MIGRATION_INVALID` (chain cycle / depth-exceeded case is reported under it). No existing code is renamed or has its underlying semantics changed. Implementations consuming the rc.13 catalog need to add the two new codes; existing codes are otherwise unchanged.

**Wire format summary.** The wire-level `spec_version` remains `"1.0"`. The closed-schema field set for the manifest's `origin` object gains one optional field (`not_after`); the manifest's top-level field set and all other document kinds are unchanged. JCS canonicalization is unchanged. NFC and other input restrictions are unchanged. Manifests omitting `origin.not_after` validate identically under rc.13 and rc.14. A manifest carrying a present `origin.not_after` is rejected by rc.13 clients (which see it as an unknown nested field and fail closed-schema discipline) and accepted by rc.14 clients subject to the new semantic constraints.

**Behavioral compatibility.** All Lotto 1 changes are tightenings: documents and behaviors valid under rc.13 remain valid under rc.14, provided implementations were not silently relying on the loose `MAY`/`SHOULD` forms now tightened. The clarified resolution-control and abandonment-semantics rules formalize behavior the prior text implied but did not enforce; conforming rc.13 clients that honored the spirit of the prior text are unaffected.

**Corpus.** `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.13"` to `"1.0-rc.14"`. Total vectors unchanged at 32; the existing vectors are unchanged byte-for-byte and validate identically under rc.14 since all rc.14 schema additions are optional and all behavioral changes are client-side. New vectors exercising `E_ORIGIN_EXPIRED`, `E_ORIGIN_INVALID`, and the migration chain-depth/cycle case are deferred to a follow-up corpus tranche, tracked separately from this rc.

### v1.0-rc.13

Date: 2026-05-10

Changes since v1.0-rc.12:

This rc lands the audit-driven hardening tranche split into two batches: tightening of `MAY`/`SHOULD` to `MUST` in §10 and §08 (Lotto 1, patch-level, no schema change), and additive features in §06, §08, §04, and the corpus (Lotto 2, additive, schema and diagnostic-catalog changes).

**Lotto 1 - patch-level tightening (§10, §08, §01)**

- §10 First contact -> TOFU pinned (W18) - Strengthened to `MUST` an explicit affirmative pinning prompt. Successful manifest verification, content rendering, dismissal of the first-contact notice, navigation away, or any other passive event MUST NOT cause the transition. The client MAY render the first-contact content before presenting the prompt; rendering does not constitute pinning. The prior `SHOULD` form, which permitted "first successful render" to trigger the transition, is removed: a client that pins on first render is now non-conformant.
- §10 PIP display requirements (W11/W12) - New normative subsection. The client MUST display the complete 24-word PIP at First contact, during Changed/mismatch resolution, in expandable detail surfaces, and anywhere the user is being asked to compare publisher identity against an out-of-band reference. Truncated, prefix-only, suffix-only, or "first-N + last-M" displays are not conformant. A 24-word BIP-39 PIP encodes the full 256-bit `K_publisher.pub` plus an 8-bit checksum; partial display reduces attacker grinding cost.
- §10 Multiple origins per publisher (W23) - `publisher_profile` strengthened to `MUST` for clients that retain trust state across sessions. A cross-session client without publisher-profile records is not conformant. Stateless clients are exempt and MUST present their statelessness in chrome.
- §10 Render dependence on manifest verification - New consolidated `MUST` (formerly distributed across §05, §06, §10): no document, block, or referenced resource is rendered until manifest signature, trust-state, canary, and origin-binding stages have completed. Chrome elements (origin, loading indicator, transport errors, verification progress) are exempt; the rule covers publisher-controlled content.
- §10 Conditional always-visible warnings - Added canary conflict warning entry to the chrome warnings list; persists until the user resolves it.
- §08 Equal `issued_at` conflict (W7) - Reframed `E_CANARY_CONFLICT` as a fault condition on the publisher identity, not a recoverable transient error. The client MUST NOT pick a deterministic "winner" between conflicting manifests by lexicographic comparison, payload size, `runtime_pubkey` value, or any other tiebreaker over manifest content; a deterministic tiebreaker is gameable by an attacker holding `K_publisher_priv` and would mask the underlying fault. The retained pre-conflict manifest stays in place for current rendering and anti-downgrade; the conflict is surfaced as a prominent chrome warning analogous to Changed/mismatch, with an option to abandon the retained publisher identity. The warning persists until the user explicitly resolves it.
- §01 - Publisher profile glossary entry now notes the conditional `MUST` for stateful clients.

**Lotto 2 - additive (§06, §10, §11, §04, §08, §02, §00, §01, corpus)**

- §06 `migration_pointer` (W22) - New optional top-level manifest field. The publisher's signed announcement of a successor carrier endpoint operated under the same `K_publisher`. When present, contains exactly `successor_origin` (mirrors `origin` schema, with `successor_origin.address` MUST differ from `origin.address` and `successor_origin.carrier` MUST equal `origin.carrier`) and `announced_at` (RFC 3339 UTC, MUST NOT be later than `updated`). Absent when no migration is announced; per §04 no-`null` discipline, encoded by omitting the field rather than by a `null` value.
- §10 Origin migration handling - New subsection. A client supporting publisher profiles MUST detect a present `migration_pointer`, display a chrome migration notice, fetch the successor manifest from `successor_origin.address`, apply Stages 1-9, and verify that `successor_manifest.publisher_pubkey` byte-equals the announcing manifest's `publisher_pubkey`. On match, the successor origin is adopted into the publisher profile. On mismatch, the announcement is rejected as `E_MIGRATION_MISMATCH`. The client SHOULD obtain user confirmation before automatic migration; a strict-mode client MAY refuse in-band migration and require out-of-band navigation, but MUST display in chrome that an announcement was present.
- §11 - Added `E_MIGRATION_MISMATCH` and `E_MIGRATION_INVALID` to the Binding diagnostics (Stage 9) catalog. `E_MIGRATION_MISMATCH` SHOULD include in `details` the announced successor address, the successor and announcing publisher keys, and the specific mismatch field. `E_MIGRATION_INVALID` covers structural well-formed but semantically invalid announcements (successor equals announcing, `announced_at` after `updated`, carrier mismatch).
- §04 Unicode normalization for user-visible strings (W6) - New normative subsection. JSON string fields whose values the client renders to the user as text MUST be encoded in NFC (Unicode Standard Annex #15). Non-exhaustive list: `canary.statement`, `meta.title`, manifest `navigation` `label`, `state_policy` `purpose`, and every block text content span and string field rendered to the user including `link.label`, `image.alt`, `code_block.content`, `feedback.statement`, `note.statement`, `submit_form` labels and option labels. NFD, NFKC, NFKD are not permitted forms. Non-NFC values rejected with `E_SCHEMA_FIELD_SYNTAX` at schema validation, before signature verification. Implementations MUST NOT silently re-normalize: re-normalization would alter the JCS canonical bytes and break the publisher's signature.
- §02 Closed-schema discipline - Made the optional-fields semantics explicit: a field is optional only if its owning section designates it optional; only fields explicitly listed (required or optional) may appear at the top level; absent values are encoded by omitting the field, never by `null`. Required for `migration_pointer` to land cleanly in the closed schema.
- §08 `freshness_proof` signaling and strict policy (W4) - Added "Client signaling on absence": the chrome MUST signal whether the current canary includes a `freshness_proof`; absence MUST NOT be silently treated as equivalent to presence. Added "Strict freshness policy": a client MAY operate in a mode that rejects manifests whose canary omits `freshness_proof`, with chrome treatment analogous to Invalid canary state. Strict policy is client-side configuration, not a manifest declaration.
- §00 - Replaced "No in-band origin migration discovery" entry with "In-band origin migration is publisher-initiated only", reflecting `migration_pointer`. Pre-existing publisher cache without a prior announcement still requires out-of-band recovery.
- §01 - Added "Migration pointer" glossary entry; updated "Manifest" entry to list `migration_pointer` among manifest fields.
- corpus - Added 4 new vectors:
  - `154-sig-non-canonical-r`: signature whose R has a non-canonical compressed encoding (y portion 2^255 - 1, exceeds the field prime). Strict profile rejects with `E_SIG_VERIFICATION` independently of the verification equation.
  - `155-sig-non-canonical-a`: `publisher_pubkey` with the same non-canonical encoding. Strict profile rejects with `E_SIG_VERIFICATION` before signature check.
  - `181-canary-issued-at-downgrade`: manifest with `canary.issued_at` strictly older than a previously verified manifest (001) for the same `K_publisher.pub`. Anti-downgrade rejects with `E_CANARY_DOWNGRADE`.
  - `190-unicode-nfd-statement`: manifest whose `canary.statement` contains a decomposed combining mark (NFD: `Cafe` + U+0301) instead of the precomposed NFC form. Schema validation rejects with `E_SCHEMA_FIELD_SYNTAX` before signature verification.
- `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.12"` to `"1.0-rc.13"`. Total vectors: 32 (was 28). The original 28 vectors are unchanged byte-for-byte: rc.13 schema and §10 changes are additive (the optional `migration_pointer` field is omitted in all existing vectors) or behavioral additions that do not affect their verdicts.

**Diagnostic catalog summary.** Two new error codes: `E_MIGRATION_MISMATCH`, `E_MIGRATION_INVALID`. No existing code is renamed or has its semantics changed. Implementations consuming the rc.12 catalog need to add the two new codes; existing codes are otherwise unchanged.

**Wire format summary.** The wire-level `spec_version` remains `"1.0"`. The closed-schema top-level field set for manifests gains one optional field (`migration_pointer`); content and transaction documents are unchanged. JCS canonicalization is unchanged. NFC validation is an input restriction at schema time, not a canonicalization rule. Manifests omitting `migration_pointer` validate identically under rc.12 and rc.13. A manifest carrying a present `migration_pointer` is rejected by rc.12 clients (which see it as an unknown top-level field and fail closed-schema discipline) and accepted by rc.13 clients.

### v1.0-rc.12

Date: 2026-05-09

Changes since v1.0-rc.11:

- §00 - Added a new v1.0 limitation entry, "No retroactive revocation of historical content," disclosing that the protocol does not distinguish documents signed before from documents signed after a `K_runtime` compromise within the same authorization window. Historical content signed by an attacker who held the compromised key during its authorization window is indistinguishable from legitimate publisher content at the protocol level.
- §05 - Extended the `K_publisher` compromise subsection in "Compromise summary" with an explicit paragraph on identity retirement: Entangled has no in-band way to retire a publisher identity key while keeping the corresponding PIP attributable. Operators who wish to permanently decommission an identity SHOULD destroy `K_publisher_priv`; the protocol cannot distinguish "publisher stopped publishing" from "publisher resumed under attacker control after a long pause."
- Repo - Added `.gitattributes` enforcing LF on `*.json`, `*.md`, and `*.py` to keep the corpus byte-for-byte reproducible across platforms. The corpus generator's `keys.json` and `corpus.json` writes were converted from `write_text` to `write_bytes` with an explicit UTF-8/LF terminator as a defensive layer in case `.gitattributes` is bypassed (e.g., editing on Windows without Git filters active). Existing `corpus/keys.json` and `corpus/corpus.json` regenerated to match.
- docs/RELEASES.md rc.11 entry - Retroactively added three bullets for changes that landed in rc.11 but were omitted from the rc.11 release notes: the §03 citation external-handoff paragraph, the README missing carrier-link bullet, and the README repo-layout and corpus documentation. No spec text was modified by this release-note correction; it is a tracking fix only.
- `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.11"` to `"1.0-rc.12"`. The 28 input vectors and their expected verdicts are unchanged byte-for-byte; the rc.12 spec changes since rc.11 are additive disclosures and do not affect any existing vector's outcome.

This rc adds no new diagnostic codes, changes no existing diagnostic semantics, and makes no wire-format or signature-input changes. Two new spec-text entries are added - one §00 limitation and one §05 compromise-summary clarification - both purely additive disclosures that codify behavior already implicit in the protocol. Repo housekeeping fixes the corpus reproducibility regression on mixed-platform clones and corrects the rc.11 release-note coverage. The wire-level `spec_version` remains `"1.0"`. The deferred rc.10 corpus follow-up - vectors exercising `E_SCHEMA_DUPLICATE_ENTRY` and `W_HISTORICAL_RUNTIME_AMBIGUOUS` - is still pending and is tracked separately from this rc.

### v1.0-rc.11

Date: 2026-05-09

Changes since v1.0-rc.10:

- §00 - Added a new v1.0 limitation entry, "Diagnostic stage selection is not constant-time." The validation pipeline in §10 emits diagnostics keyed to the first failing stage, and the structured diagnostic itself names that stage (§11). A natural sequential implementation therefore exhibits observable timing differences across rejection causes, and the diagnostic is itself an explicit channel. A passive observer with timing access, or any consumer of the structured diagnostic, may infer information about a document's failure mode. Entangled v1 does not require constant-time diagnostic emission and does not place this in the protocol's threat model. Whether a future protocol version should constrain this side channel is acknowledged as open and may be revisited.
- §03 - Added a citation handoff trust-boundary callout. After the existing external-handoff sentence, a paragraph notes that opening a citation transmits the URL to a browser and a network outside Entangled's carrier; the destination operators, any in-path clearnet observer, and the chosen browser may learn that the URL was reached from the user's local environment, along with whatever metadata each layer collects. SHOULD-level UX guidance: the handoff mechanism should make this trust boundary visible before navigation proceeds. The wire schema, the existing MUSTs around citation handling (no auto-navigation, distinct display, no request state), and the HTTPS-only / no-`http://`-permitted rule are unchanged.
- README - Added the `carrier` link kind to the "Links are explicitly typed" list, alongside `same_site`, `entangled`, and `citation`. The README description matches §03's no-auto-navigate + external-handoff treatment for `carrier`. The kind has been defined in §03 since the carrier-link work; the omission predated rc.11.
- README - Repository structure listing now names `corpus/` (with a sub-tree pointer to `corpus/README.md`) and `docs/RELEASES.md`. The omission predated rc.11 and was carried over from the pre-corpus README.
- Corpus - Added `publisher.pip` to `corpus/keys.json`: the 24-word Publisher Identity Phrase derived from `publisher.pub_b64u` per §05 (BIP-39 English wordlist over the raw 32-byte public key with an 8-bit SHA-256 checksum). An implementation deriving PIPs MUST produce the same string for this public key. The canonical BIP-39 English wordlist is bundled at `corpus/tools/bip39_english.txt` (sourced from `bitcoin/bips: bip-0039/english.txt`, SHA-256 `2f5eed53a4727b4bf8880d8f3f199efc90e58503646d9ff8eff3a2ed3b24dbda`); `compute_pip()` and `load_bip39_wordlist()` helpers are added to the generator. The expected PIP value was cross-verified byte-for-byte against an independent BIP-39 reference implementation.
- Corpus - Added a normative paragraph to `corpus/README.md` describing how negative vectors are constructed: after all earlier stages pass cleanly, exactly one diagnostic-relevant violation is intended to be live at the first failing stage. This is the principle that drove the rc.9 fixes for vectors 132 and 142 and the rc.9 reclassification of 151; it is now stated explicitly as a corpus design rule so that future tranches preserve diagnostic determinism.
- Corpus - Strengthened the docstring on `corpus/tools/generate.py`'s `jcs()` helper, which is currently a thin wrapper over Python's `json.dumps(sort_keys=True, separators=(",", ":"), ensure_ascii=False)`. The new note enumerates the specific edge cases - non-ASCII member names (UTF-16 vs codepoint sort order divergence), non-integer numerics (RFC 8785 §3.2.2.3 ECMA-262 number serialization), and Python-specific escaping divergences - that require replacing the helper with a verified RFC 8785 implementation before the corpus can grow to cover them. No current vector exercises any of those cases, so the bundled `jcs()` is sufficient for the rc.11 corpus.
- `corpus/corpus.json` - `rc_target` bumped from `"1.0-rc.9"` to `"1.0-rc.11"` to reflect that the corpus is now consistent with both the rc.10 spec-text fixes and the rc.11 §00 limitation and PIP additions. The 28 input vectors and their expected verdicts are unchanged byte-for-byte from rc.9; the rc.11 spec changes since rc.9 are additive and do not affect any existing vector's outcome.

This rc adds no new diagnostic codes, changes no existing diagnostic semantics, and makes no wire-format or signature-input changes. The wire-level `spec_version` remains `"1.0"`. The deferred rc.10 corpus follow-up - vectors exercising `E_SCHEMA_DUPLICATE_ENTRY` and `W_HISTORICAL_RUNTIME_AMBIGUOUS` - is still pending and is tracked separately from this rc.

### v1.0-rc.10

Date: 2026-05-09

Changes since v1.0-rc.9:

- §11 - Moved `E_TRUST_MISMATCH` and `E_TRUST_USER_REJECTED` from "Trust state diagnostics (Stage 7)" to a new heading "Trust state diagnostics (Stage 6 manifest pre-check and Stage 7 resolution)". The diagnostic codes themselves are unchanged in spelling and semantics; only the catalog grouping and the `stage` field of structured diagnostics for these two codes change (from `7` to `6`). `I_TRUST_FIRST_CONTACT`, `I_TRUST_TOFU_PINNED`, and `I_TRUST_VERIFIED` remain Stage 7 transitions. This resolves the prior contradiction between §10's pre-check description and §11's Stage 7 grouping. The catalog framing sentence at the top of §11 is updated to note the Trust state group's Stage 6/7 span.
- §10 - Added an inline note to the Stage 6 line of the pipeline summary clarifying that manifest signature verification has two sub-steps (identity pre-check then Ed25519 verify), while content/transaction signature verification uses `current_manifest.canary.runtime_pubkey`. Aligned the Stage 7 summary line to describe only First contact / TOFU pinning / external-verification transitions (mismatch detection is handled in the Stage 6 pre-check). The detailed Stage 6 and Stage 7 prose is unchanged.
- §06 - Replaced the redundant restatement of path-syntax bullets in the navigation `path` constraint with a pure reference to §02, including the reservation of `/manifest.json` (which the prior restatement omitted). The local cross-host/cross-origin/absolute-URL prohibition sentence is preserved.
- §09, §10 - Aligned `request_id` no-reuse language. §09 now uses MUST for both cross-temporal no-reuse (including retries) and concurrent-in-flight no-reuse, matching the existing §10 MUST. The two requirements are split into explicit sentences under "Collision avoidance"; the schema-side `request_id` paragraph defers to that subsection rather than restating the MUST, removing a now-redundant phrasing. Verbal strength is consistent across §09 and §10.
- §11 - Added `E_SCHEMA_DUPLICATE_ENTRY` to the Stage 5 schema diagnostics catalog, severity `error`, document_kind `any`. Covers within-array uniqueness violations: duplicate `(namespace, key)` in `state_policy` (§07), duplicate field `name` in a `submit_form` (§03), duplicate `value` in `select.options` (§03), and duplicate marks in inline `marks` (§03). The structured diagnostic SHOULD include `field_path` and the duplicated value in `details`. Previously these were forced into `E_SCHEMA_FIELD_SYNTAX` or `E_SCHEMA_ENUM_VIOLATION`, neither of which fit. §07 and §03 now reference the new code by name at each uniqueness rule.
- §09 - Added an explicit 4096-byte cap on `request_state[].value` in the submit body schema, restating the protocol's absolute state-value ceiling (§07). Publishers MAY reject larger values as malformed submits.
- §11, §10 - Added `W_HISTORICAL_RUNTIME_AMBIGUOUS` to the Historical content diagnostics catalog, severity `warning`, document_kind `content`. Covers the case in which historical content signature verifies under more than one distinct retained `K_runtime.pub` for the same `K_publisher.pub` - an outcome with probability ~2^-256 indicating cryptographic anomaly, implementation bug, or authorization-history corruption. Severity is `warning` because the document is rejected per §10 but the condition does not invalidate other content. §10 now references this code where it previously said "client-implementation diagnostic".
- README, §06 - Replaced the term "site entry" with "site" everywhere it appeared (README trust-state table, §06 `publisher_pubkey` prose). The term was not defined in §01 and the distinction between "site entry" and "site" was unintentional.
- §10 - For `manifest.updated` future-skew rejection, added a note that the structured `E_SCHEMA_FIELD_SYNTAX` diagnostic SHOULD include `reason: "future_beyond_skew_tolerance"` and the offending timestamp in `details`, distinguishing temporal-domain failures from lexical RFC 3339 violations. No new diagnostic code is added; `E_CANARY_INVALID` remains the dedicated code for canary `issued_at` future skew.
- §03 - Added a canonical "All seven of `kind`, `src`, `sha256`, `media_type`, `width`, `height`, and `alt` are required. `caption` is optional. No other top-level fields are permitted." sentence to the image block schema. Clarified that `alt` MAY be the empty string for purely decorative images, in contrast to `caption` where the empty string is forbidden.
- §01 - Added glossary entries for "Submit body" (referenced from §09), "Origin binding" (the verification rule defined in §05 and §06), and "Image resource" (distinct from the `image` block; defined in §03 and §09). Cross-references updated on related entries.
- §10 - Removed a redundant chrome-display sentence under Expired-canary-block mode that duplicated the general "When a reduced mode is active, the client MUST display the mode in chrome." rule. The mode-specific notice about the replaced content area is preserved; only the duplicated chrome-display MUST is removed.
- §09 - Added an explicit clarifying sentence after the status-code whitelist table noting that codes outside the whitelist (including `204`, `304`, `418`) are treated as transport or protocol errors rather than as Entangled semantics, except for `3xx` redirects which are explicitly rejected.
- docs/design-decisions.md - Added a banner near the top noting that the file's vocabulary and trust-model summary are pre-spec snapshots; authoritative definitions live in §01 and §05; when the document and the numbered specification differ, the numbered specification governs.

This rc adds two new diagnostic codes (`E_SCHEMA_DUPLICATE_ENTRY`, `W_HISTORICAL_RUNTIME_AMBIGUOUS`); the rest of the catalog is unchanged in spelling and semantics. `E_TRUST_MISMATCH` and `E_TRUST_USER_REJECTED` move from Stage 7 to Stage 6 in the catalog grouping (the codes themselves are unchanged; only the `stage` field of structured diagnostics for these two codes changes from `7` to `6`). Implementations consuming the rc.9 diagnostic catalog need to add the two new codes and update their Stage attribution for the two trust codes; existing codes are otherwise unchanged in semantics. Corpus vectors exercising the affected within-array uniqueness rules and the multi-key historical trial anomaly should be updated to assert the new codes; that corpus update is out of scope for this rc and will be tracked in a follow-up tranche.

The wire format, signature inputs, and signature input construction are unchanged. The wire-level `spec_version` remains `"1.0"`.

### v1.0-rc.9

Date: 2026-05-08

Changes since v1.0-rc.8:

- §11 - Added `E_SCHEMA_ENUM_VIOLATION` to the stage 5 schema diagnostics catalog. The code applies when a field whose value is required to be one of an enumerated set carries a syntactically valid value not in that set: for example a block `kind` slug not in the enumerated block kinds (§03), an unknown state-policy `mode`, or an unknown transaction `feedback` `variant`. Previously such cases were forced into `E_SCHEMA_FIELD_SYNTAX`, which conflated lexical-form violations (slug grammar, base64url alphabet, RFC 3339 form) with set-membership violations.
- §11 - Reformulated the `E_SIG_MALFORMED` row to make the §04 / §05 precedence boundary explicit. When the `sig` field is received on the wire, length and base64url-alphabet violations are reported as `E_SCHEMA_FIELD_SYNTAX` at stage 5 per §04 and §10's first-failing-stage rule; `E_SIG_MALFORMED` covers signature decoding contexts where stage-5 wire-side field-syntax validation does not apply. No semantic change.
- corpus - Added a top-level `clock_now` field (`"2026-05-07T00:01:00Z"`) to `corpus.json`. Harnesses MUST mock the implementation's wall clock to this value for the duration of the test run. Canary diagnostics depend on `now` against fixed `issued_at` timestamps; without clock mocking, time-dependent vectors drift into `W_CANARY_EXPIRED` or `E_CANARY_INVALID` as real time advances. `corpus/README.md` documents the requirement and adds it as a step to the harness pattern.
- corpus - Vectors 132 (`schema-null-value`) and 142 (`numeric-overflow`) now carry all required manifest fields. Previously six required fields were omitted, allowing `E_SCHEMA_REQUIRED_FIELD` to compete with the targeted diagnostic at stage 5; with the fix, the `null` literal (132) and the 2^63 overflow (142) are unambiguously the only stage-5 violations. Diagnostic codes are unchanged.
- corpus - Vector 133 (`schema-block-kind-unknown`) diagnostic changes from `E_SCHEMA_FIELD_SYNTAX` to `E_SCHEMA_ENUM_VIOLATION`, reflecting the new §11 code.
- corpus - Vector 151 renamed from `sig-malformed-length` to `sig-syntax-length`, diagnostic changes from `E_SIG_MALFORMED` to `E_SCHEMA_FIELD_SYNTAX`. The `sig` field is 43 ASCII characters instead of the canonical 86; the §04 declared-length check at stage 5 fires before §05 stage-6 signature decoding under §10's first-failing-stage rule, so the prior `E_SIG_MALFORMED` diagnostic was unreachable.
- corpus/README.md - Added a coverage note: this initial corpus exercises representative diagnostic codes per pipeline stage and does not cover every code in the §11 catalog. Future tranches will fill out the remaining codes.

This rc adds one new diagnostic code (`E_SCHEMA_ENUM_VIOLATION`) and corrects two corpus-vector diagnostic mappings (133 -> `E_SCHEMA_ENUM_VIOLATION`; 151 -> `E_SCHEMA_FIELD_SYNTAX`). Vectors 132 and 142 keep their codes but are now unambiguously isolated to the targeted violation. Implementations that consumed the rc.8 corpus need to update their diagnostic mapping for vectors 133 and 151 and must mock the wall clock to `corpus.json["clock_now"]`. The wire format, signature inputs, signature input construction, and the rest of the diagnostic code catalog are unchanged.

The wire-level `spec_version` remains `"1.0"`.

### v1.0-rc.8

Date: 2026-05-08

Changes since v1.0-rc.7:

- Added a normative conformance corpus under `corpus/`. A v1.0-conforming implementation MUST agree with the verdict (accept or reject + diagnostic) recorded for each vector. The corpus is generated deterministically from fixed test seeds by `corpus/tools/generate.py` (Python 3.10+ with `cryptography`).
- Initial corpus: 28 vectors covering manifest, content, and transaction documents. 5 positive (must accept) and 23 negative (must reject), spanning input checks (BOM, bad UTF-8), JSON parsing (duplicate keys), kind / spec_version, schema (unknown field, missing required, null, unknown block kind), numeric grammar (float, exponent, overflow), signatures (modified payload, malformed length, non-canonical S, small-order A), strict base64url (padding, alphabet, whitespace), binding (path mismatch, reserved `/manifest.json`, request_hash), and canary (equal-`issued_at` conflict). Each vector carries a description, spec references, and a normative diagnostic code from §11.
- §04 - Replaced the "distributed separately" disclaimer for the conformance corpus with a normative reference to `corpus/`. The illustrative single-object JCS test vector remains.

The wire-level `spec_version` remains `"1.0"`. The corpus does not change protocol behavior; it makes the existing rules testable across implementations.

### v1.0-rc.7

Date: 2026-05-08

Changes since v1.0-rc.6:

- §00 - Added a "v1.0 limitations" subsection that consolidates protocol-level limitations a v1.0 implementation should disclose to users: no in-band runtime-key revocation, canary expiration is not cryptographic revocation, no general anti-replay against malicious backends, no historical-content bootstrap for new clients, no in-band origin-migration discovery, no protection from a malicious publisher, image decoding is a residual attack surface. Each bullet references the section that defines the underlying rule.
- §03 - Added a "Decoder safety" subsection. Hash verification authenticates image bytes against the signed document but does not make decoding safe; a publisher with a valid `K_runtime` may sign a document referencing an intentionally crafted image. Implementations SHOULD use memory-safe decoders, hardened parsers, sandboxed processes, or other isolation appropriate to the deployment environment. Protocol-level rejections (media-type allowlist, no SVG, no animation, hash verification, dimension limits, pixel budget) are necessary but not sufficient.
- §07 - Strengthened the request-state consent prompt: the client MUST explain that the item is included in future submit requests across every submit endpoint under the publisher's identity, not only the current form or endpoint. Made explicit that request-state scope in v1 is publisher-wide and endpoint-scoped request state is not part of v1.
- §10 - Strengthened the First contact -> TOFU pinned transition from MAY to SHOULD. The recommended trigger is the user explicitly choosing to continue to the site or the first successful render of content. The client SHOULD document, in user-accessible form, the trigger it uses. Other triggers (e.g. dismissal of the first-contact notice) remain permitted.
- §10 - Added a clock-skew UX hint. When rejecting a timestamp because it exceeds the 300-second tolerance, the client SHOULD indicate to the user that the local clock may be incorrect, since clock skew is a likely cause of false positives. The protocol-level diagnostic remains the one specified for the failing field; the advisory is a presentation hint, not a separate diagnostic code.

This rc is editorial. It does not change the wire format, signature inputs, signature input construction, or the diagnostic code catalog.

The wire-level `spec_version` remains `"1.0"`.

### v1.0-rc.6

Date: 2026-05-08

Changes since v1.0-rc.5:

- §02 - Reserved `/manifest.json` at the protocol level. The path is now explicitly forbidden as a content document `path`, transaction `in_response_to`, image `src` (§03), submit endpoint, and inline-link target. Added a bullet to each path-syntax restatement.
- §02 - Clarified the scope of the `request_id` / `request_hash` binding: they bind the signed transaction response to the submit body the client sent, but are not a general anti-replay mechanism against a malicious or compromised publisher backend, which may still receive, store, or reuse submit bodies and sign transaction responses for any submit body it accepts.
- §03 - Replaced the abstract image-resource verification list with a precise, ten-step ordered sequence that names the specific diagnostic for each step (`W_IMAGE_FETCH_FAILED`, `W_IMAGE_CONTENT_TYPE`, `W_IMAGE_OVERSIZE`, `W_IMAGE_HASH_MISMATCH`, `W_IMAGE_DECODE_FAILED`, `W_IMAGE_DIMENSIONS`, `W_IMAGE_BUDGET`). Made explicit that the declared `media_type` is authoritative for decoder selection while the response `Content-Type` is checked separately for header consistency.
- §03 - Added a normative WebP animation detection requirement. A client MUST determine animation status before rendering, by inspecting the RIFF chunk structure or by querying its decoding library. An implementation whose WebP library cannot expose this property reliably MUST reject all WebP resources or disable WebP support. Silently rendering only the first frame of an animated WebP is non-conformant.
- §08 - Refined the equal-`issued_at`-not-a-conflict criterion to depend solely on the JCS-canonical signed payload rather than on byte-for-byte wire equivalence and signature equality. Under deterministic Ed25519 (RFC 8032) signing the same payload under the same key produces an identical `sig`, so the canonical-payload criterion subsumes the signature criterion; framing it as canonical payload makes the protocol-level invariant clear.
- §10 - Added a "Verification key trial order" section for historical content. The order in which retained `K_runtime.pub` entries are tried is implementation-defined (reverse chronological by first-observed `issued_at` recommended). Verification under more than one distinct retained key for the same document is treated as a cryptographic anomaly, an implementation bug, or authorization-history corruption; the client MUST reject the document. No new normative error code; clients SHOULD log the condition.

This rc clarifies path reservation, image verification ordering, and historical-content trial order, and scopes the submit-binding guarantees against backend replay. None of these changes alter the wire format, signature inputs, signature input construction, or the diagnostic code catalog.

The wire-level `spec_version` remains `"1.0"`.

### v1.0-rc.5

Date: 2026-05-08

Changes since v1.0-rc.4:

- §04 - Added an explicit ABNF integer grammar (`integer = "0" / non-zero-digit *digit`) and a parser-level enforcement rule. Numeric tokens MUST be validated lexically before any conversion to a numeric type. JSON parsers that convert numeric tokens to IEEE 754 binary64 first are non-conforming for Entangled use because they cannot reliably distinguish `42` from `42.0` or `1e0`, lose precision above `2^53` (so `9007199254740993` becomes `9007199254740992`), and conflate `-0` with `+0`. Implementations MUST use a parser that either exposes raw numeric tokens for lexical inspection, or rejects non-integer tokens at parse time, or perform a separate raw-bytes validation pass. The integer's decimal value MUST be in `[0, 2^63 - 1]`.
- §04 - Added a "Strict base64url decoding" section that pins decoder behavior for every base64url-encoded field in the protocol (`sig`, `publisher_pubkey`, `origin_pubkey`, `runtime_pubkey`, `expected_publisher_pubkey`, `request_id`, `image.sha256`, `transaction.request_hash`). The decoder MUST use only the URL-safe alphabet (`A-Z`, `a-z`, `0-9`, `-`, `_`), reject every character outside it including `+`, `/`, whitespace, line breaks, and non-ASCII characters, reject the padding character `=`, enforce the field-declared exact ASCII length, and reject non-canonical trailing-group encodings (the unused bits in the final encoded character MUST be zero). Permissive decoders that accept padded input, ignore whitespace, accept the standard `+`/`/` alphabet, or accept non-canonical trailing-group encodings are non-conforming.

This rc tightens parsing strictness without changing the wire format, signature inputs, signature input construction, or the diagnostic code catalog. Documents that an rc.4 client accepted by virtue of a permissive JSON parser or base64 decoder may be rejected by an rc.5 client; conforming current parsers and decoders are unaffected.

The wire-level `spec_version` remains `"1.0"`.

### v1.0-rc.4

Date: 2026-05-08

Changes since v1.0-rc.3:

- §02 - Clarified that `request_hash` is computed over the JCS-canonical bytes of the parsed submit body, not over the wire bytes received. The formula already pinned this; the prose previously read "the exact submit body bytes received, after JCS-canonicalization", which was ambiguous. Insignificant whitespace and member ordering in the wire submit body are now stated explicitly to not affect the hash.
- §05 - Added an "Ed25519 verification profile" section that pins the strict, cofactorless validation rules. Public keys MUST be in canonical encoding and MUST NOT be small-order; signatures MUST use canonical `R` and canonical `S` (`0 <= S < L`); verification uses the cofactorless equation `[S]B = R + [k]A`. The profile aligns with `verify_strict` in `ed25519-dalek`. Implementations MUST NOT use cofactored verification or accept non-canonical encodings. This eliminates cross-implementation divergence in signature acceptance, in particular between libraries that historically split between RFC 8032 permissive and ZIP-215-style strict modes.
- §07 - Replaced the "opaque byte strings" terminology for state values with "opaque UTF-8 strings", aligning the safe-display subsection with the value field schema (`value` is a UTF-8 string). Editorial only; no wire-format change.
- §09 - Added a "Content-Encoding and Transfer-Encoding" section that forbids both headers in both directions. Publishers MUST NOT use `Content-Encoding` on any Entangled response or `Transfer-Encoding` (including `chunked`) on any response; clients MUST disable automatic HTTP-layer decompression and reject responses carrying either header. The same rules apply to submit `POST` request bodies. Closes the interop ambiguity that arose when transport stacks decompressed responses by default, which would change the byte sequence used for the byte cap (§02, §06, §10), for `Content-Length` consistency checks, and for the SHA-256 digest of image resources (§03). Adds a normative implementation note that conforming clients must use an HTTP stack whose default header injection and decompression can be disabled.
- §11 - Added `E_TRANSPORT_CONTENT_ENCODING` and `E_TRANSPORT_TRANSFER_ENCODING` to the Stage 1 transport diagnostics catalog.

This rc tightens transport and signature-verification behavior. Responses or signatures that an rc.3 client might have accepted under HTTP-stack default decompression or under cofactored Ed25519 verification can now be rejected; conforming current signers and HTTP stacks are unaffected. The wire-level `spec_version` remains `"1.0"`; the rc number tracks pre-release stabilization, not protocol identity (§11).

### v1.0-rc.3

Date: 2026-05-08

Changes since v1.0-rc.2:

- §03 - Added a fourth link target kind, `target.kind = "carrier"`, for linking to non-Entangled services reachable through an Entangled-supported carrier (for example, non-Entangled Tor onion services). The `carrier` kind takes `carrier` and `url` fields, accepts only `http://` URLs (the carrier provides confidentiality and integrity at the rendezvous layer, and the destination identity is anchored at the carrier address itself), and shares the no-auto-navigate and no-request-state disciplines of `citation` while additionally requiring a carrier-aware browser handoff.
- §03 - Replaced the placeholder note on `citation`'s `http://` ban ("a future protocol version may revisit this...") with an explicit redirect: non-clearnet destinations belong under `kind: "carrier"`, not under `kind: "citation"`.
- §01 - Added the **Carrier link** glossary entry; updated the **Citation link**, **Entangled link**, **Link**, and **Same-site link** entries to reference the new kind. The **Link** entry now describes four target kinds, not three.
- §09 - Clarified that the transport rules govern requests issued by the Entangled client to Entangled endpoints. They do not constrain how the client hands off non-Entangled URLs (`target.kind = "carrier"` or `"citation"`) to external components such as Tor Browser or a system browser.

This rc adds a value to a closed-schema enum (`target.kind`). A document using `kind:"carrier"` is rejected by rc.1 and rc.2 clients during schema validation. The wire-level `spec_version` remains `"1.0"`; the rc number tracks pre-release stabilization, not protocol identity (§11).

## Relationship to the specification

This document describes engineering process. It does not define protocol behavior. The authoritative protocol specification lives in the `specs/` directory; this file does not override or modify it.
